# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: 2026-06-23
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| Live smoke operator run plan | `deploy/governance/live-smoke-operator-run-plan.contract.json` and `npm run check:live-smoke-operator-run-plan` now bind the credentialed operator flow to the current preflight output, six live-smoke commands, required env names, auth sources, capture templates, output packet paths, cleanup requirement, and current release readiness follow-up checks | A credentialed operator can execute the next live-smoke slice without reconstructing command order or packet mapping from chat history, but the plan still does not run live smoke, create passed packets, or unlock Sprint 0.4 / Phase 0 | When a credentialed operator is ready, run `npm run check:live-smoke-external-env-preflight`, `npm run check:live-smoke-operator-run-plan`, the six smoke commands, then `npm run check:live-smoke-capture-packets`, `npm run check:live-smoke-capture-transition-review`, `npm run check:live-smoke-evidence-ledger`, and `npm run check:current-release-readiness` before changing any gate |
| Current release readiness checklist | `deploy/release-checklists/current-release-readiness.contract.json` and `npm run check:current-release-readiness` now summarize sprint completion audit, sprint exit gate transition review, mainline publication readiness, and the 8 completion blocker manifests into one current PR/release decision surface | The repo can now prove the current state is not PR/release-ready while external/live/frontend blockers remain open, but this does not create credentialed live-smoke packets, partner data evidence, frontend evidence, or external signed approvals | Start with credentialed live-smoke capture packets by running `npm run check:live-smoke-external-env-preflight`, then rerun `npm run check:live-smoke-capture-packets`, `npm run check:live-smoke-capture-transition-review`, `npm run check:live-smoke-evidence-ledger`, and `npm run check:current-release-readiness` before changing any Sprint/Phase gate or opening a PR |
| Gate0 signed evidence packet intake readiness | `deploy/governance/gate0-signed-evidence-manifest.contract.json` now has an `intake_readiness` block and `deploy/governance/gate0-signed-evidence-packets/README.md` now names the 6 external signed evidence packet files, required approver roles, accepted packet conditions, acceptance commands, and forbidden payload policy | External Gate0 signed evidence can now be accepted without guessing from chat history or raw legal/vendor/commercial/privacy artifacts, but no Gate0 packet has landed and external approvals, manifest promotion, transition review, Sprint 0.1, and Phase 0 remain blocked | When Gate0 signed evidence packets arrive, copy each redacted hash-only packet into `deploy/governance/gate0-signed-evidence-packets/`, keep raw legal memos, raw contracts, raw vendor documents, raw payloads, credentials, database URLs, connection strings, account/workspace IDs, tokens, secrets, env values, and unredacted source payloads out, then rerun `npm run check:gate0-signed-evidence-packets`, `npm run check:gate0-signed-evidence-packet-fixtures`, `npm run check:gate0-signed-evidence-handoff`, `npm run check:gate0-signed-evidence-manifest`, `npm run check:gate0-signed-evidence-manifest-fixtures`, `npm run check:gate0-signed-evidence-transition-review`, `npm run check:gate0-signed-evidence-transition-review-fixtures`, `npm run check:sprint-completion-audit`, and `npm run check:sprint-exit-gate-transition-review` before changing Sprint or Phase checkbox state |
| Sprint 1 live data evidence packet intake readiness | `deploy/governance/sprint1-live-data-evidence-manifest.contract.json` now has an `intake_readiness` block and `deploy/governance/sprint1-live-data-evidence-packets/README.md` now names the 9 external/operator packet files, blocking surfaces, required evidence, approver roles, accepted packet conditions, acceptance commands, and forbidden payload policy | External live-data evidence can now be accepted without guessing from chat history or raw partner/DB/billing artifacts, but no Sprint 1 live-data packet has landed and live Serving reads, SQL execution, usage writes, billing reconciliation, manifest gates, and activation gates remain false | When Sprint 1 live-data evidence packets arrive, copy each redacted hash-only packet into `deploy/governance/sprint1-live-data-evidence-packets/`, keep raw partner rows, raw database values, raw billing payloads, database URLs, connection strings, account/workspace/invoice/customer IDs, tokens, secrets, and env values out, then rerun `npm run check:sprint1-live-data-evidence-packets`, `npm run check:sprint1-live-data-evidence-packet-fixtures`, `npm run check:sprint1-live-data-evidence-handoff`, `npm run check:sprint1-live-data-activation`, `npm run check:sprint1-live-data-evidence-manifest`, `npm run check:sprint1-live-data-evidence-manifest-fixtures`, `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint-completion-audit`, and `npm run check:sprint-exit-gate-transition-review` before changing Sprint or Phase checkbox state |
| Frontend release evidence packet intake readiness | `deploy/governance/frontend-release-evidence-handoff.contract.json` now has an `intake_readiness` block and `deploy/governance/frontend-release-evidence-packets/README.md` now names the 6 frontend packet files, Sprint/requirement mapping, accepted hash fields, acceptance commands, and forbidden payload policy; `agent_ask_progress_ui.evidence.json`, `agent_evidence_card_ui.evidence.json`, `comparison_screening_ui.evidence.json`, `research_library_ui.evidence.json`, and `developer_console_ui.evidence.json` are now accepted with hash-only desktop/mobile route, accessibility, build, test, and redaction evidence | Frontend evidence can now be accepted without guessing from chat history or raw artifacts; 5 of 6 frontend surfaces are accepted, but `frontend_release_surfaces_complete` remains false while WCAG 2.1 AA packet is still missing | When remaining frontend evidence packets arrive, copy each redacted hash-only packet into `deploy/governance/frontend-release-evidence-packets/`, keep raw screenshots/DOM/HTML/prompts/responses/secrets/account/workspace/payment/contact identifiers out, then rerun `npm run check:frontend-release-evidence-packets`, `npm run check:frontend-release-evidence-packet-fixtures`, `npm run check:frontend-release-evidence-handoff`, `npm run check:p0-open-requirement-transition-review`, `npm run check:sprint-completion-audit`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:mainline-publication-readiness` before changing Sprint or §M checkbox state |
| WCAG frontend public-ops evidence blocker routing | Sprint 3.2's `wcag_frontend_public_ops_evidence_missing` is now represented by the existing `frontend_release_evidence` completion blocker manifest instead of a free-text manual condition | Sprint 3.2 no longer needs a duplicate manual WCAG/public-ops frontend blocker, but it remains blocked by one backlog item and missing WCAG 2.1 AA frontend evidence packets | WCAG 2.1 AA evidence packets, public operations frontend evidence, Sprint 3.2 backlog rows, or frontend release transition evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:frontend-release-evidence-packets`, `npm run check:public-ops`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 3.2 or Phase 3 |
| Multilingual event research live outputs blocker routing | Sprint 3.1's `multilingual_event_research_live_outputs_missing` is now represented by the existing `frontend_release_evidence` and `sprint1_live_data_evidence` completion blocker manifests instead of a free-text manual condition | Sprint 3.1 no longer needs a duplicate manual multilingual/research live-output blocker, but it remains blocked by missing frontend multilingual/research UI evidence packets and the Sprint 1 live-data evidence packet/manifest/activation gates for partner rows and Serving SQL | Multilingual/research UI evidence packets, partner serving rows, Serving SQL execution evidence, Sprint 3.1 backlog rows, or frontend/Sprint1 live-data transition evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:localized-response`, `npm run check:research-run-save`, `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 3.1 or Phase 3 |
| Developer Console live E2E blocker routing | Sprint 2.3's `developer_console_live_e2e_missing` is now represented by the existing `mcp_target_client_live_e2e` and `frontend_release_evidence` completion blocker manifests instead of a free-text manual condition; `/developer-console` and `developer_console_ui.evidence.json` now close the frontend surface-specific requirement | Sprint 2.3 no longer needs a duplicate manual Developer Console live E2E blocker; its backlog is now 11 / 11, but it remains blocked by missing target-client E2E packets and linked MCP release gates, so the exit gate stays `☐` | Target-client E2E packets, live Console/log/usage/API-key/OAuth evidence, Sprint 2.3 row changes, or MCP/frontend transition evidence changes, then rerun `npm run check:mcp-target-client-live-e2e-transition-review`, `npm run check:frontend-release-evidence-handoff`, `npm run check:frontend-release-evidence-packets`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 2.3 or Phase 2 |
| Research library frontend blocker routing | Sprint 2.2's `research_library_frontend_evidence_missing` is represented by the existing `frontend_release_evidence` completion blocker manifest, and `/research` plus `research_library_ui.evidence.json` now close the surface-specific requirement | Sprint 2.2 no longer needs a duplicate manual research-library frontend blocker; `sprint-exit-gate-transition-review` now treats `required_frontend_surface_ids=["research_library_ui"]` as enough to complete Sprint 2.2, so its backlog is 9 / 9 and exit gate is `☑`; Phase 2 remains blocked by 2.3/2.4 plus full frontend/MCP/Sprint2.4 live-operation blockers | Research library UI evidence packet status, Sprint 2.2 backlog rows, or frontend release transition evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:frontend-release-evidence-packets`, `npm run check:sprint-exit-gate-transition-review`, `npm run check:sprint-completion-audit`, and `npm run check:current-release-readiness` before changing Sprint 2.2 or Phase 2 |
| Comparison screening frontend blocker routing | Sprint 2.1's `comparison_screening_frontend_evidence_missing` is now represented by the existing `frontend_release_evidence` completion blocker manifest instead of a free-text manual condition; the local `/analysis` comparison/screening workbench closes the last Sprint 2.1 backlog item and `comparison_screening_ui.evidence.json` is accepted | Sprint 2.1 no longer needs a duplicate manual comparison/screening frontend blocker; `sprint-exit-gate-transition-review` now treats `required_frontend_surface_ids=["comparison_screening_ui"]` as enough to complete Sprint 2.1, so its backlog is 9 / 9 and exit gate is `☑`; Phase 2 remains blocked by 2.2/2.3/2.4 plus full frontend/MCP/Sprint2.4 live-operation blockers | Comparison/screening UI evidence packets, Sprint 2.1 backlog rows, or frontend release transition evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:frontend-release-evidence-packets`, `npm run check:sprint-exit-gate-transition-review`, `npm run check:sprint-completion-audit`, and `npm run check:current-release-readiness` before changing Sprint 2.1 or Phase 2 |
| Internal account/eval frontend blocker routing | Sprint 1.4's `internal_account_eval_frontend_live_wiring_missing` is now represented by the existing `frontend_release_evidence` and `sprint1_live_data_evidence` completion blocker manifests instead of a free-text manual condition | Sprint 1.4 no longer needs a duplicate manual frontend/live-data wiring blocker, but it remains blocked by missing frontend evidence packets and the Sprint 1 live-data evidence packet/manifest/activation gates for billing reconciliation, live usage, and serving data | Frontend account/eval/workbench/quota packets, account/eval UI evidence, billing reconciliation evidence, or Sprint 1 live-data activation evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:authorized-session-memory`, `npm run check:eval-v1`, `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 1.4 or Phase 1 |
| Production user run frontend blocker routing | Sprint 1.3's `production_user_run_streaming_persistence_frontend_missing` is now represented by the existing `frontend_release_evidence` and `phase0_live_smoke_evidence` completion blocker manifests instead of a free-text manual condition; `/ask`, `agent_ask_progress_ui.evidence.json`, and `agent_evidence_card_ui.evidence.json` now close the surface-specific frontend requirement | Sprint 1.3 no longer needs a duplicate manual frontend persistence blocker, but it remains blocked by missing P0 requirement evidence packets, linked live model/tool-loop release gates, and the live-smoke ledger surfaces for credentialed model, persistence, observability, and UI-adjacent release proof | P0 AGT-01/AGT-07 transition evidence, AI Gateway/live streaming proof, user-run persistence proof, or live-smoke ledger evidence changes, then rerun `npm run check:frontend-release-evidence-handoff`, `npm run check:p0-open-requirement-transition-review`, `npm run check:live-smoke-capture-transition-review`, `npm run check:agent-live-model-streaming-release-gate`, `npm run check:agent-user-run-persistence-release-gate`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 1.3 or Phase 1 |
| Partner source rows signoff blocker routing | Sprint 1.2's `partner_source_rows_data_platform_signoff_missing` is now represented by the existing `sprint1_live_data_evidence` completion blocker manifest instead of a free-text manual condition | Sprint 1.2 no longer needs a duplicate manual partner-source blocker, but it remains blocked by the Sprint 1 live-data evidence packet/manifest/activation gates for signed partner contract, partner serving rows, and quality-owner cutover | Signed partner contract, partner serving rows, data-platform signoff, or quality-owner cutover evidence changes, then rerun `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint1-live-data-evidence-handoff`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 1.2 or Phase 1 |
| Partner live Serving/usage blocker routing | Sprint 1.1's `partner_rows_live_serving_usage_writes_missing` is now represented by the existing `sprint1_live_data_evidence` completion blocker manifest instead of a free-text manual condition | Sprint 1.1 no longer needs a duplicate manual live Serving/usage blocker, but it remains blocked by backlog items and the Sprint 1 live-data evidence packet/manifest/activation gates for partner rows, Serving SQL, usage writes, and billing reconciliation | Partner rows, Serving SQL, usage write, usage ledger, or billing reconciliation evidence changes, then rerun `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint1-live-data-activation`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 1.1 or Phase 1 |
| Credentialed live smoke blocker routing | Sprint 0.4's `credentialed_live_smoke_captures_missing` is now represented by the existing `phase0_live_smoke_evidence` completion blocker manifest instead of a free-text manual condition | Sprint 0.4 no longer needs a duplicate manual live-smoke blocker, but it remains blocked by backlog items, empty capture packets, and the live-smoke evidence ledger surfaces that still require credentialed Cloudflare/AI Gateway/OTLP/provider-secret evidence | Credentialed live smoke packets or ledger evidence changes, then rerun `npm run check:live-smoke-capture-transition-review`, `npm run check:live-smoke-evidence-ledger`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 0.4 or Phase 0 |
| Partner model output corpus blocker routing | `deploy/agent/model-output-corpus-release-gate.contract.json` is now registered as the `agent_model_output_corpus_release_gate` completion blocker manifest, so Sprint 0.3's partner-approved production corpus blocker is tracked as a release-gate manifest instead of a free-text manual condition | Sprint 0.3 no longer depends on a manual `partner_approved_production_corpus_missing` string, but the model-output-corpus release gate remains blocked by partner-approved corpus evidence, production/live generated-answer sampling, persistent eval writes, and frontend evidence cards | Partner-approved model output corpus evidence, production sampling evidence, persistent eval-write evidence, or frontend evidence-card proof changes, then rerun `npm run check:agent-model-output-corpus-release-gate`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 0.3 |
| Sprint 0.x Gate0 manual blocker manifest routing | `deploy/governance/sprint-exit-gate-transition-review.contract.json` now routes Sprint 0.1 external rights/regulatory signatures, Sprint 0.2 partner signed data contract, and Sprint 0.3 commercial cost review through existing `gate0_signed_evidence` / `sprint1_live_data_evidence` blocker manifests instead of free-text manual blocking conditions; `deploy/governance/sprint-completion-audit.contract.json` now records `sprint1_live_data_evidence` as blocking Sprint 0.2 too | The repo no longer needs separate manual text blockers for those three Gate0/Sprint1 evidence surfaces, but Sprint 0.1 remains backlog-incomplete and Sprint 0.2/0.3 remain blocked by missing signed evidence / live-data evidence plus the dedicated Agent model output corpus release gate | Any Sprint 0.x blocker manifest id, Gate0 signed evidence packet, Sprint1 signed partner data contract evidence, commercial settlement evidence, or partner approved model-output corpus evidence changes, then rerun `npm run check:sprint-exit-gate-transition-review`, `npm run check:sprint-completion-audit`, and the owner transition checks before changing Sprint 0.1/0.2/0.3 exit gates |
| Phase 3 security/load/DR release evidence handoff | `deploy/governance/phase3-security-load-dr-release-evidence-manifest.contract.json`, packet/templates directories, `npm run check:phase3-security-load-dr-release-evidence-*`, and `npm run check:phase3-security-load-dr-release-transition-review` now convert Sprint 3.3's security/load/DR release signoffs from a manual blocker into 7 machine-readable evidence gates | The repo can now accept future redacted hash-only evidence for compliance/legal/security signoff, live kill-switch/incident/audit evidence, live performance SLO, live load-test artifact, live DR restore/failover/rollback, live incident/status communications drill, and Ops/SRE/Product signoff without accidentally completing Sprint 3.3; current packet directory is empty and all transition decisions remain blocked | When any Phase 3 release evidence arrives, copy the matching template into `deploy/governance/phase3-security-load-dr-release-evidence-packets/`, keep raw security reports/APM logs/incident transcripts/audit exports/secrets out, rerun `npm run check:phase3-security-load-dr-release-evidence-packets`, `npm run check:phase3-security-load-dr-release-transition-review`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 3.3 or Phase 3 exit gates; accepted evidence packet alone cannot complete Sprint 3.3 |
| Sprint manual blocker owner map | `deploy/governance/sprint-exit-gate-transition-review.contract.json` now has an empty `manual_blocker_owner_map` because all manual blocking conditions have been routed to completion blocker manifests; `npm run check:sprint-exit-gate-transition-review` still validates that any future manual blocker must have an owner manifest, owner check commands, and closure evidence refs | Manual blockers are gone from the active Sprint/Phase exit review, but the same evidence gaps remain represented by blocker manifests: Gate0 signed evidence, phase0 live smoke, Sprint1 live data, Agent model output corpus, MCP target-client E2E, frontend release evidence, Sprint 2.4 live operations, and Phase 3 security/load/DR release evidence | Any manual blocking condition is added, removed, renamed, or moved between Sprints, then update `manual_blocker_owner_map`, rerun `npm run check:sprint-exit-gate-transition-review`, `npm run check:sprint-completion-audit`, and the owner check commands before changing exit gates |
| Sprint blocker coverage audit | `deploy/governance/sprint-completion-audit.contract.json` and `npm run check:sprint-completion-audit` now derive each blocker manifest's `blocks` list from `deploy/governance/sprint-exit-gate-transition-review.contract.json`, so the top-level audit cannot under-report which Sprint/Phase exit gates are blocked by Gate0 signed evidence, live smoke evidence, Sprint 1 live data evidence, Agent model output corpus release gate, MCP target-client E2E, frontend release evidence, Sprint 2.4 live operations evidence, or Phase 3 security/load/DR release evidence | The repo now catches drift between the blocker ledger and the actual sprint-exit transition review, but this still only proves blocker accounting; all referenced evidence packets/live checks remain missing or blocked | Any blocker manifest id, Sprint/Phase `blocker_manifest_ids`, or top-level `blocks` list changes, then rerun `npm run check:sprint-exit-gate-transition-review` and `npm run check:sprint-completion-audit` before claiming a Sprint/Phase exit gate |
| Sprint 2.4 live operations evidence handoff | `deploy/governance/sprint2-4-live-operations-evidence-manifest.contract.json`, packet/templates directories, `npm run check:sprint2-4-live-operations-evidence-*`, and `npm run check:sprint2-4-live-operations-transition-review` now convert Sprint 2.4's full-backlog-but-open exit gate from a manual blocker into 13 machine-readable evidence gates | The repo can now accept future redacted hash-only evidence for paid billing provider, subscription/audit/invoice/ledger live writes, invoice/proration/refund preview, high-cost pre-debit/refund, live Workflow/deep-report execution, watchlist/saved-screening/correction fanout, MCP live auth credential store, kill-switch live flag source, and Claude-owned frontend UI without accidentally completing Sprint 2.4; current packet directory is empty and all transition decisions remain blocked | When any Sprint 2.4 live-operation evidence arrives, copy the matching template into `deploy/governance/sprint2-4-live-operations-evidence-packets/`, keep raw billing/workflow/notification/credential material out, rerun `npm run check:sprint2-4-live-operations-evidence-packets`, `npm run check:sprint2-4-live-operations-transition-review`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint 2.4 or Phase 2 exit gates |
| Sprint exit gate transition review | `deploy/governance/sprint-exit-gate-transition-review.contract.json`, `npm run check:sprint-exit-gate-transition-review`, and `npm run check:sprint-exit-gate-transition-review-fixtures` now parse the tracker sprint/phase rows and prove backlog count alone cannot complete a Sprint; each Sprint exit gate needs complete backlog, clear linked blocker manifests, and empty manual blocking conditions, and each Phase gate needs all child Sprint transitions allowed | The repo can now catch drift where a full `9 / 9`, `10 / 10`, `13 / 13`, or `17 / 17` backlog row is mistaken for completion, but it still does not replace external signatures, live smoke captures, Sprint 1 live data packets, target-client E2E packets, Claude-owned frontend evidence, billing/workflow live evidence, or final release signoffs | Any Sprint backlog row, Phase/Sprint exit gate, blocker manifest, or manual blocking condition changes, then rerun `npm run check:sprint-exit-gate-transition-review`, `npm run check:sprint-exit-gate-transition-review-fixtures`, and `npm run check:sprint-completion-audit` before claiming Sprint completion |
| Sprint blocker transition audit | `deploy/governance/sprint-completion-audit.contract.json` and `npm run check:sprint-completion-audit` now validate the 8 blocker manifests plus their linked transition review/release-gate checker surfaces for Gate0 signed evidence, live-smoke capture, Sprint 1 live data, Agent model output corpus release gate, MCP target-client E2E, P0/frontend evidence, Sprint 2.4 live operations evidence, and Phase 3 security/load/DR release evidence; each linked transition must stay wired into package scripts/root check, keep `release_transition_allowed=false`, and retain `not_claimed=all_sprints_complete` while evidence is missing | The top-level sprint audit can now catch manifest/transition drift, but it still cannot replace external signatures, credentialed live-smoke captures, Sprint 1 live data packets, target-client E2E packets, partner-approved output corpus evidence, Claude-owned frontend evidence, billing/workflow live evidence, or final security/load/DR release signoffs | Any blocker manifest, transition review, packet directory, Sprint table row, or §M checkbox changes, then rerun the specific transition checker plus `npm run check:sprint-completion-audit` before claiming Sprint completion |
| Live smoke capture transition review | `deploy/governance/live-smoke-capture-transition-review.contract.json`, `npm run check:live-smoke-capture-transition-review`, and `npm run check:live-smoke-capture-transition-review-fixtures` now prove a passed capture packet alone cannot complete Sprint 0.4; each live smoke needs both a validated hash-only packet and a passed ledger surface before release transition | The repo can accept future redacted capture packets without accidentally completing live smoke, but current packet directory is empty and all transition decisions remain blocked | After operator capture packets land under `deploy/governance/live-smoke-capture-packets/`, rerun `npm run check:live-smoke-capture-packets`, `npm run check:live-smoke-capture-transition-review`, `npm run check:live-smoke-evidence-ledger`, and `npm run check:sprint-completion-audit` before changing Sprint 0.4 checkbox state |
| Sprint 1 live data transition review | `deploy/governance/sprint1-live-data-transition-review.contract.json`, `npm run check:sprint1-live-data-transition-review`, and `npm run check:sprint1-live-data-transition-review-fixtures` now prove an accepted evidence packet alone cannot complete Sprint 1 live data; each gate needs a validated hash-only packet, accepted manifest gate, and accepted activation gate | The repo can accept future redacted Sprint 1 evidence packets without accidentally completing live Serving, usage writes, billing reconciliation, or partner source rows, but current packet directory is empty and all transition decisions remain blocked | After operator evidence packets land under `deploy/governance/sprint1-live-data-evidence-packets/`, rerun `npm run check:sprint1-live-data-evidence-packets`, `npm run check:sprint1-live-data-transition-review`, `npm run check:sprint1-live-data-activation`, `npm run check:sprint1-live-data-evidence-manifest`, and `npm run check:sprint-completion-audit` before changing Sprint 1.1 or 1.2 checkbox state |
| A4 production/live unsourced numeric sampling | Local deterministic sampling is now checked by `docs/governance/unsourced-numeric-sampling-gate.md`, `deploy/observability/unsourced-numeric-sampling.contract.json`, and `npm run check:unsourced-numeric-sampling`, but production/live sampling remains incomplete because no live model output corpus, partner-approved production corpus, persistent eval writes, or frontend evidence cards are available | The repo can enforce 1000 accepted deterministic samples plus 3 blocked probes against `observed_rate < 0.001`, but cannot claim the PRD production sampling SLO | Live model outputs, partner-approved corpus, and eval-store persistence are available, then replace fixture-only sampling with captured production samples and keep `npm run check:unsourced-numeric-sampling` as the release guard |
| External Gate 0 approvals after local MVP boundary-copy closure: rights matrix, HKEX/vendor role, Type 4 written opinion, PCPD path, commercial terms, signatures | MVP product-boundary copy, Gate 0 external-evidence intake, signed evidence manifest, manifest transition fixtures, operator handoff templates, signed evidence packet verifier, and Gate 0 signed evidence transition review are now locally checked (`npm run check:mvp-product-boundary-copy`, `npm run check:gate0-external-evidence-intake`, `npm run check:gate0-signed-evidence-manifest`, `npm run check:gate0-signed-evidence-manifest-fixtures`, `npm run check:gate0-signed-evidence-handoff`, `npm run check:gate0-signed-evidence-packets`, `npm run check:gate0-signed-evidence-packet-fixtures`, `npm run check:gate0-signed-evidence-transition-review`, `npm run check:gate0-signed-evidence-transition-review-fixtures`), but the remaining Gate 0 items require data partner, legal/compliance, business, privacy, and executive evidence outside this repo | Phase 1 market-data and MCP surfaces remain blocked; docs-only packet/intake/manifest/handoff/transition contracts use `DEFAULT_DENY` / `DATA_NOT_LICENSED`, keep all six manifest packets/templates missing, the packet directory empty, and all transition decisions blocked, and do not replace signed rights matrix, Type 4 written opinion, PCPD approval, commercial terms, or Gate 0 signatures | Signed Gate 0 evidence arrives or leadership records a signed waiver/fallback, then copy the matching template into `deploy/governance/gate0-signed-evidence-packets/`, run `npm run check:gate0-signed-evidence-packets` and `npm run check:gate0-signed-evidence-packet-fixtures`, promote accepted redacted refs into `deploy/governance/gate0-signed-evidence-manifest.contract.json`, and rerun `npm run check:gate0-signed-evidence-handoff`, `npm run check:gate0-signed-evidence-manifest`, `npm run check:gate0-signed-evidence-manifest-fixtures`, `npm run check:gate0-external-evidence-intake`, `npm run check:gate0-signed-evidence-transition-review`, and `npm run check:sprint-completion-audit`; accepted packet alone cannot complete Gate 0 |
| Partner-signed data contract and real source samples | Current data methodology is a baseline, not a signed contract | Physical schema and ingestion must avoid assuming partner field names/SLAs | Partner provides signed contract, field dictionary, source IDs, delivery samples, and SLA |
| Production partner golden corpus and cost-reviewed quality/commercial gate | Synthetic v0 fixtures and deterministic quality rules now pass locally, but they are not partner-approved source samples and do not meet PRD §10.7 production volume | `DATA_QUALITY_HOLD` is executable in the fixture gate, but production data correctness, rights coverage, and unit economics remain unproven | Partner sample corpus, source hashes, data-owner sign-off, and real cost review arrive |
| Engineering runtime remaining surfaces: model provider live execution smoke, Cloudflare resource provisioning/smoke, live Hyperdrive `SELECT 1`, OTLP live export + persistent eval write/read smoke, provider secret store provisioning/rotation smoke | Runtime scaffold now covers npm workspaces, Hono Worker health, shared data contracts, CI, env contract, env validation, Cloudflare binding contract, AI SDK dry-run Agent Runtime skeleton, model provider/streaming guard contract, local run.audit/run.eval telemetry events, eval-store/OTLP destination contract guard, Supabase-compatible migration tooling, provider secret-store contracts/runbooks, live-smoke non-secret resource defaults (`scripts/lib/live-smoke-defaults.mjs`, `npm run check:live-smoke-defaults`), live smoke external-env preflight (`npm run check:live-smoke-external-env-preflight`), live smoke capture artifacts contract plus capture packet verifier/fixtures/operator handoff templates (`deploy/governance/live-smoke-capture-artifacts.contract.json`, `deploy/governance/live-smoke-capture-packets/`, `deploy/governance/live-smoke-capture-templates/`, `npm run check:live-smoke-capture-artifacts`, `npm run check:live-smoke-capture-packets`, `npm run check:live-smoke-capture-packet-fixtures`, `npm run check:live-smoke-capture-handoff`), live smoke evidence ledger and transition fixtures (`deploy/governance/live-smoke-evidence-ledger.contract.json`, `npm run check:live-smoke-evidence-ledger`, `npm run check:live-smoke-evidence-ledger-fixtures`), and the verified integrated `@aiphabee/web` TanStack Start/Vite baseline with Design System tokens/components/assets | Phase 0 can run a Worker locally and validate dry-run agent policy, model-provider guard, local telemetry, eval-store destination guard, DB migration contracts, secret-store contracts, live-smoke default env inference, per-command external-env readiness, live smoke capture artifact schema, strict packet-directory validation, capture packet fixtures, operator handoff templates, live smoke evidence accounting/transition semantics, and the web baseline through typecheck/tests/build, but cannot claim staging deployability, live data access, real model execution completeness, live telemetry export, managed secret operations, WCAG AA, or live frontend/backend data wiring; current preflight reports `ready_commands=0`, `blocked_commands=6`, and missing non-inferable account/token/model, OTLP endpoint/headers, and GitHub/Supabase project env | Provide live credentials/external env and rerun `npm run check:live-smoke-external-env-preflight` plus `npm run check:live-smoke-capture-handoff`, then `npm run smoke:cloudflare-resources-live`, `npm run smoke:cloudflare-bindings-wrangler-live`, `npm run smoke:ai-gateway-live`, `npm run smoke:ai-gateway-observability-live`, `npm run smoke:observability-live`, and `npm run smoke:provider-secret-stores-live`; capture redacted outputs from the templates as `<capture_id>.capture.json` under `deploy/governance/live-smoke-capture-packets/`, run `npm run check:live-smoke-capture-packets` and `npm run check:live-smoke-capture-packet-fixtures`, then update the live smoke evidence ledger with hash-only evidence refs |
| Sprint 1.1 live data surfaces: partner-loaded securities master/raw snapshots, financial facts, corporate actions, real Serving Gateway, field entitlements, usage ledger, account/workspace model | Data Access Gateway default-deny, quality-hold, synthetic field-entitlement guards, entitlement DB policy-source compiler, synthetic financial restatement cases, synthetic corporate-action adjustment cases, Serving read planner, Serving quality release isolation planner, Serving quality live-readiness gate, Sprint 1.1 live data activation ledger/evidence manifest/evidence packet handoff (`deploy/governance/sprint1-live-data-activation.contract.json`, `deploy/governance/sprint1-live-data-evidence-manifest.contract.json`, `deploy/governance/sprint1-live-data-evidence-packets/`, `deploy/governance/sprint1-live-data-evidence-templates/`, `docs/governance/sprint1-live-data-activation.md`, `docs/governance/sprint1-live-data-evidence-manifest.md`, `npm run check:sprint1-live-data-activation`, `npm run check:sprint1-live-data-evidence-manifest`, `npm run check:sprint1-live-data-evidence-manifest-fixtures`, `npm run check:sprint1-live-data-evidence-packets`, `npm run check:sprint1-live-data-evidence-packet-fixtures`, `npm run check:sprint1-live-data-evidence-handoff`), Serving query planner, Serving SQL descriptor/text compiler, Serving execution adapter, Serving result envelope, and Usage Ledger event writer planner are executable; security master/raw snapshot, financial fact/restatement, corporate-action/adjustment, account/workspace entitlement, usage ledger, and Serving Store schemas now exist, but no partner data, live Serving SQL execution/rows/writes, live DB entitlement reads, live usage writes, or billing reconciliation are wired | Gateway can reject unsafe access, compile entitlement row snapshots, plan blocked/held reads, map quality state to Serving release posture, verify `HOLD/REJECT_RAW` stop before Serving query/sql/execution, plan released snapshot query material, plan SQL descriptor bindings/text, defer execution with empty rows, expose a no-live result envelope, plan per-call usage events, and now block live activation unless signed data contract, partner rows, live policy source, Hyperdrive `SELECT 1`, quality-owner cutover, usage-event live writes, ledger-entry live writes, and billing reconciliation live reads have redacted hash-only evidence accepted in the manifest; it still cannot return real licensed data or persist usage accounting | Provide signed partner data contract and live activation evidence, copy the matching templates into `deploy/governance/sprint1-live-data-evidence-packets/`, then run `npm run check:sprint1-live-data-evidence-packets`, `npm run check:sprint1-live-data-evidence-packet-fixtures`, `npm run check:sprint1-live-data-evidence-handoff`, `npm run check:sprint1-live-data-activation`, `npm run check:sprint1-live-data-evidence-manifest`, `npm run check:sprint1-live-data-evidence-manifest-fixtures`, `npm run check:field-rights-live-policy-source`, `npm run check:serving-quality-live-readiness`, and `npm run check:usage-billing-reconciliation` before enabling live Serving/usage writes |
| Sprint 1.2 tool execution and evidence surfaces: registered tool handlers, precise schemas, golden fixtures, MCP/API endpoints, Evidence/Lineage service | Shared Tool Registry scaffold now centralizes all 16 PRD §9.2 P0 read-only tools; the original no-live synthetic handlers remain route-backed, the later P0 catalog consistency gate aligns registry/schema/MCP/agent/evidence/golden surfaces, local input/output schema contracts exist, MCP runtime schema snapshot serving is scaffolded (`GET /mcp/runtime/tool-schemas`, `deploy/mcp/runtime-schema-snapshot.contract.json`, `npm run check:mcp-runtime-schema-snapshot`), tool golden fixtures run in `test:golden`, the no-write Evidence/Lineage service planner/schema are scaffolded, local Worker server-orchestrated route replay now runs all 16 fixtures through `apps/worker/src/tool-route-replay.test.ts` / `deploy/governance/sprint1-tool-route-replay.contract.json`, MCP protocol tool execution smoke now runs guarded `POST /mcp` `tools/call` through a real Worker tool route (`deploy/mcp/protocol-tool-execution-smoke.contract.json`, `npm run check:mcp-protocol-tool-execution-smoke`), Evidence live DB write smoke now verifies guarded Hyperdrive insert/read/delete for `core.evidence_record` + `core.evidence_source_ref` (`deploy/evidence/live-db-write-smoke.contract.json`, `npm run check:evidence-live-db-write-smoke`), and partner source row evidence packet gate routing now links the remaining blocker to `partner_serving_rows_loaded` in the Sprint 1 live-data evidence manifest (`npm run check:sprint1-live-data-evidence-manifest`, packet checker); the tool route replay readiness gate (`deploy/governance/sprint1-tool-route-replay-readiness.contract.json`, `npm run check:tool-route-replay-readiness`, fixture checker) now machine-records the remaining blocker against that packet gate; live partner rows remain absent | Agent runtime can reject unregistered tools; `/tools/runtime` reports registry metadata; `/mcp/runtime/tool-schemas` reports 16 Tool Registry descriptor schema snapshots with strict input and structured output metadata; `POST /mcp` `tools/list` reports schema snapshot summary while default-denied and full schema snapshots when rights are confirmed; guarded `POST /mcp` `tools/call` smoke proves MCP live protocol execution with pre-execution rights/scope/revocation checks; guarded `POST /evidence/records/live-db-smoke` proves Evidence table insert/read/delete through Hyperdrive with hash-only response and no partner rows; partner source rows are gated by the hash-only, redacted, raw-row-forbidden `partner_serving_rows_loaded` evidence packet; tool routes return synthetic data/scope/lineage/events where route scaffolds exist; `/evidence/runtime` reports schema-ready no-write service capability; `/evidence/records/plan` links tool calls to source records/data versions/methodologies/user-visible citations without SQL or DB writes; `check:tool-schemas` validates 16 schema IDs/envelope/error enum; `test:golden` validates 8 quality samples and 16 tool response fixtures; `check:p0-tool-catalog` cross-checks all 16 P0 names across registry/schema/MCP/agent/golden contracts; `check:tool-route-replay` proves route replay canonical projection; `check:mcp-protocol-tool-execution-smoke` proves the guarded MCP protocol tool execution smoke; `check:evidence-live-db-write-smoke` proves the guarded Evidence live DB write smoke; `check:tool-route-replay-readiness` keeps `release_transition_allowed=false` until the remaining partner-source blocker has accepted evidence | Revisit when partner source records and data-platform signoff are authorized |
| Sprint 1.3 Agent runtime surfaces: context, ToolLoopAgent, streaming progress, evidence binding, answer structure, failure recovery, model routing | Agent run context, no-model ToolLoopAgent planner, backend SSE progress stream, guarded Agent tool execution + evidence-binding smoke, guarded Agent model execution audit smoke, guarded Agent live ToolLoop orchestration smoke, guarded Agent generated-answer evidence-binding smoke, guarded Agent run live write smoke, guarded Agent run state persistence smoke, guarded Agent billing-posted ledger smoke, no-write Agent user-run persistence release gate, no-write AI Gateway observability release gate, no-write Agent live model streaming release gate, no-write Agent user ToolLoop execution release gate, no-write Agent model output corpus release gate, no-write Agent token/cost/fallback release gate, pre-tool-call resolution, budget/stop policy, tool enforcement, numeric source guard, answer/evidence contract, failure recovery policy, and model routing audit scaffolds now cover run/user/workspace, subscription plan, synthetic default-deny entitlements, registry version, registered/versioned/schema-bound/permission-aware tool checks, no arbitrary SQL/URL, tool versions/schema/scope, budget dimensions, dry-run model tier, phased tool planning, max-3 parallel read-only chunks, public progress event contracts, `text/event-stream` no-model run/tool progress emission, stop/retry policy, security/time/currency/methodology preflight, assumptions, blocking clarification for critical security ambiguity, deterministic budget estimates, `stopped_budget`, partial graceful stop response, continuation cost, two-consecutive same-class error retry stop, no-source concrete financial number blocking, PRD 8.3 ordered answer sections, evidence-card payload fields, failed-tool-call-only retry, completed evidence reuse, partial answer fallback, usage-ledger idempotency placeholder, no-double-charge retry billing, Cloudflare AI Gateway model execution preview, hash-only smoke responses, a guarded transactional audit/evidence/usage ledger insert-read-delete proof, a guarded run-state/checkpoint insert-read-update-delete proof, a guarded usage ledger preview-to-posted idempotency proof, a no-write production user-run persistence release gate over those three smoke proofs, a no-write AI Gateway observability release gate over model audit/readiness/capture/ledger evidence surfaces, a no-write live model streaming release gate over backend SSE progress plus guarded `streamText`/evidence/observability proof surfaces, a no-write user ToolLoop execution release gate over planner/preflight/enforcement/budget/recovery/fixed-smoke/persistence proof surfaces, a no-write model output corpus release gate over local sampling/evidence/model-audit/streaming/eval/live-smoke-ledger surfaces, and a no-write token/cost/fallback release gate over model audit/routing/audit-field/AI-Gateway-observability/billing/user-run-persistence surfaces; no arbitrary user ToolLoop execution, user-facing run live model token streaming, live model output corpus, production/live generated-answer sampling, AI Gateway log/cost/cache/rate-limit/fallback accepted evidence, live token-cost-fallback log writes, production user-run audit/evidence/ledger/state persistence cutover, production billing provider/invoice posting, or frontend Ask/evidence-card rendering exists | `/agent/runtime` reports run-context, ToolLoopAgent planner, backend progress stream transport, preflight, budget-stop, tool-enforcement, numeric-source guard, answer-evidence, failure-recovery, model-routing-audit readiness, user-run persistence release-gate readiness, AI Gateway observability release-gate readiness, live model streaming release-gate readiness, user ToolLoop execution release-gate readiness, model output corpus release-gate readiness, and token/cost/fallback release-gate readiness; `/agent/runs/dry-run` returns complete no-model context and rejects unregistered SQL/URL tools; `/agent/runs/stream` returns public SSE progress events with no prompt text, chain-of-thought, model calls, or actual tool execution; `/agent/release-gates/user-run-persistence/plan` links the live-write, state-persistence, and billing-posted ledger smoke contracts while keeping `production_cutover_allowed=false`; `/agent/release-gates/ai-gateway-observability/plan` links model execution audit, AI Gateway observability, model readiness, capture packet, and evidence ledger surfaces while keeping `release_transition_allowed=false`; `/agent/release-gates/live-model-streaming/plan` links backend SSE progress, guarded model audit `streamText`, guarded live ToolLoop `streamText`, generated-answer evidence binding, and AI Gateway observability while keeping `release_transition_allowed=false`; `/agent/release-gates/user-tool-loop-execution/plan` links planner, preflight, tool enforcement, budget/stop, failure recovery, fixed tool execution evidence, fixed live ToolLoop, and user-run persistence gates while keeping `release_transition_allowed=false`; `/agent/release-gates/model-output-corpus/plan` links unsourced numeric sampling, generated-answer evidence binding, model execution audit, live model streaming gate, eval v1, and live-smoke evidence ledger while keeping `release_transition_allowed=false` and `production_sampling_enabled=false`; `/agent/release-gates/token-cost-fallback/plan` links model execution audit, model-routing audit, run/tool audit fields, AI Gateway observability, billing-posted ledger, and user-run persistence gates while keeping `release_transition_allowed=false`, `production_cost_ledger_enabled=false`, and `live_token_cost_fallback_log_writes=false`; guarded Agent smoke routes now cover tool execution/evidence binding, model execution audit, fixed ToolLoop orchestration, generated-answer evidence binding, live-write audit/evidence/usage rows, run-state persistence, and billing-posted ledger idempotency; `check:agent-tool-execution-evidence-smoke`, `check:agent-model-execution-audit-smoke`, `check:agent-ai-gateway-observability-release-gate`, `check:agent-live-model-streaming-release-gate`, `check:agent-user-tool-loop-execution-release-gate`, `check:agent-model-output-corpus-release-gate`, `check:agent-token-cost-fallback-release-gate`, `check:agent-live-tool-loop-smoke`, `check:agent-generated-answer-evidence-smoke`, `check:agent-run-live-write-smoke`, `check:agent-run-state-persistence-smoke`, `check:agent-billing-posted-ledger-smoke`, `check:agent-user-run-persistence-release-gate`, `check:agent-run-context`, `check:tool-loop-agent`, `check:pre-tool-call-resolution`, `check:budget-stop-policy`, `check:tool-enforcement`, `check:numeric-source-guard`, `check:answer-evidence-contract`, `check:failure-recovery-policy`, and `check:model-routing-audit` guard the shapes | Revisit when arbitrary user ToolLoop execution, user-facing live model streaming, live model output corpus, AI Gateway accepted read/capture/cost/fallback evidence, live token-cost-fallback writes, production user-run persistence cutover, production billing provider/invoice posting, or web Ask/evidence-card integration starts |
| Sprint 1.4 workbench/account/eval surfaces | Eval v1 + WVRO backend scaffold now covers `run.eval.eval_v1`, `GET /observability/runtime` eval capability, `POST /observability/eval-v1/plan`, fact/calculation/citation/correct-refusal metrics, unsourced numeric target `<0.1%`, and WVRO four criteria/high-intent actions; internal account/session/manual-plan scaffold now covers `@aiphabee/account-runtime`, `GET /account/runtime`, `POST /account/session/plan`, supported login method contract, session/device revoke plans, manual plan assignment plans, forbidden credential payload classes, and account/workspace/subscription table linkage; usage quota display backend scaffold now covers `GET /usage/runtime`, `POST /usage/quota/plan`, Web Agent/MCP channels, request_id, plan_code, period, credit limit, used/pending/remaining credits, and 5-minute freshness target; stock workbench aggregate scaffold now covers `GET /workbench/runtime`, `POST /workbench/stock/snapshot`, `POST /workbench/stock/announcements`, security profile, quote snapshot, price history, financial facts, derived metrics, announcement search, corporate actions, data-quality statuses, evidence summary, and ambiguous security blocking; STK-04 profitability metrics are computed deterministically and valuation multiples are explicitly blocked with `market_cap_unavailable` when market cap/share-count authority is absent; STK-06 basic announcement search returns source locators without live original document fetch; frontend rendering, live identity provider, cookie issuance, DB writes, billing provider integration, live usage ledger reads, billing reconciliation, persistent eval writes, live OTLP export, full Phase 2 announcement/document tools, and production partner-approved eval corpus remain absent | The repo can now deterministically calculate eval v1/WVRO eligibility, plan internal account/session/manual-plan changes, compute quota display snapshots, and aggregate all six Sprint 1.4 stock workbench backend sections without live reads/writes or frontend dashboards, but cannot claim an internal user can complete end-to-end research until frontend integration and live dependencies land | Revisit when Claude returns frontend workbench/account/quota surfaces for integration, or when Phase 2 full announcement/document search tools start |
| Sprint 2.1 comparison and deterministic analytics | `compare_securities` backend scaffold now covers `@aiphabee/analytics-tools`, `GET /analytics/runtime`, `POST /analytics/compare-securities`, 2-5 security input, security resolution, profile/quote/financial facts fanout, base currency/unit, missing metrics, quality flags, source record IDs, and explicit incomparable reasons; `screen_securities` backend scaffold now covers deterministic natural-language parsing, editable structured conditions, field/operator/value/time-basis/missing-value/source-tool display, confirmation-before-live-execution semantics, synthetic preview hits, `why` explanations, rejected-row reasons, explainable ranking, and a point-in-time future-classification guard with `classification_as_of <= as_of`; `get_financial_ratios` backend scaffold now covers deterministic accounting ratios, formula version, source record IDs, blocked reasons, and synthetic percentile metadata; `calculate_returns_risk` backend scaffold now covers deterministic return/risk metrics, explicit-benchmark Beta, formula version, golden tolerance, source record IDs, and window metadata; `compare_percentiles` backend scaffold now covers peer/index/history percentile comparison, benchmark_as_of, constituent_as_of, point-in-time policy, percentile rank, and source record IDs; `plan_high_cost_analytics` backend scaffold now covers screen/compare PRD credit-weight estimates, `analytics_high_cost` independent pool routing, ordinary pool protection, confirmation, pre-debit/failure-refund metadata, idempotency, and no-write enqueue planning; `/analysis` now provides a local comparison/screening Web workbench over mock envelopes, including 2-5 IPO compare, editable structured screening conditions, why explanations, rejected reasons, and confirmation-before-live-execution copy; ratios/returns-risk/percentile UI, MCP registration, live FX conversion, live historical constituents/industry/names, live peer/benchmark constituents, durable queue writes, live usage-ledger debits/refunds, accepted frontend evidence packets, and live data remain absent | The repo can now compare a small synthetic security set, preview editable screens with explanations in the web app, block future classification metadata for historical screens, compute deterministic ratio rows, compute deterministic return/risk rows, compare synthetic peer/index/history percentiles, and plan high-cost analytics onto a separate pool without live writes; Sprint 2.1 backlog is now 9 / 9, but its exit gate remains blocked by missing frontend release evidence | Revisit when the comparison/screening frontend evidence packet is accepted or when live queue/usage-ledger/MCP limiter work starts in later sprints |
| Sprint 2.2 announcement and research surfaces | `search_announcements` backend scaffold now covers `@aiphabee/document-tools`, `GET /documents/runtime`, `POST /documents/search-announcements`, company/security resolution, published_at range, category, keyword, language, limit filters, title/date/category/language/summary result rows, `document_id`, `source_record_id`, page/anchor synthetic locators, untrusted document policy metadata, and ambiguous security blocking; `get_announcement` backend scaffold now covers `POST /documents/get-announcement`, `document_id` lookup, optional sections, bounded authorized excerpts, page/paragraph/source_record synthetic locators, and no-fabrication for missing documents/sections; `document_sanitizer` now covers announcement-excerpt script/hidden text/suspicious instruction removal with audit metadata and document-origin tool invocation disabled; `search_documents` now covers synthetic pgvector-style chunk ranking, metadata filter pushdown, sanitized snippets, similarity scores, and page/paragraph/source locators; `diff_announcements` now covers FY2023/FY2024 synthetic annual-results comparison, revenue/operating_profit schema-bound extraction, `announcement_numeric_extraction_v0` validation, base/comparison absolute/percent change, and page/paragraph/source locators for every extracted value and diff side; `save_research_run` now covers question/tool input/evidence/model/prompt/parameter snapshots, immutable `snapshot_id`, replay seed, no-write persistence plan, and old-report mutation disabled metadata; `replay_research_run` now covers saved/current run no-write replay planning, data/model/parameters diff classification, preserved old snapshot IDs, and silent rewrite blocking; `/research` now renders the synthetic research library with hash-only accepted evidence; live pgvector index/SQL execution, Vectorize, original document fetch, full document return, webpage/user-input sanitizer coverage, live parser extraction, live DB/R2 research-run writes, and live replay execution remain absent | The repo can now search synthetic announcements, retrieve bounded citation excerpts, sanitize document-origin malicious instructions, run deterministic semantic chunk retrieval, compare schema-bound filing numbers, plan complete immutable research-run snapshots, compare saved/current run snapshots, and render the no-live research library; Sprint 2.2 is 9 / 9 with exit gate `☑`, while Phase 2 remains blocked by other Sprint/frontend/MCP/live-operation gates | Revisit when live DB/R2 persistence, live replay execution, or frontend release-wide completion evidence is authorized |
| Sprint 2.3 Remote MCP OAuth and Developer Console | `mcp_endpoint_default_deny` backend scaffold now covers `@aiphabee/mcp-runtime`, `GET /mcp/runtime`, `POST /mcp`, planned `initialize` / `tools/list` / `tools/call` method handling, Origin validation, Gate 0 MCP/API redistribution default-deny, Web rights separation from MCP machine-readable rights, empty `tools/list` while rights are unconfirmed, and `DATA_NOT_LICENSED` for blocked `tools/call`; `mcp_oauth_pkce` now covers `GET /mcp/oauth/runtime`, authorize/token/revoke plan routes, PRD §9.7 scope catalog, PKCE `S256`, clear revocable scope consent, no-live token exchange, third-party token passthrough blocked, and revoke future-call denial; `mcp_api_key` now covers `GET /mcp/api-keys/runtime`, create/rotate/revoke plan routes, server-to-server only posture, hash-only storage metadata, one-time display metadata, raw key rejection, IP allowlist validation, rotation old-key future-call denial, and revoke future-call denial; `mcp_tool_schema_validation` now covers `tools/call` argument object validation, required/anyOf field validation, additionalProperties=false rejection, input/output schema IDs, and structuredContent/outputSchema no-live validation metadata; `mcp_tool_versioning` now covers public `tool@1` versions, active deprecation metadata, 90-day minimum notice, 180-day previous-major support policy, and breaking-change-new-major policy; `mcp_pagination_limits` now covers Tool Registry retrieval metadata, `tools/list` retrieval limit descriptors, `tools/call` `bounded_retrieval`, cursor metadata, max-row rejection, and time-window rejection before live execution; `mcp_usage_envelope` now covers response `usage` request_id/remaining credits, `tools/call` `usage_envelope`, estimated credits, no-live usage ledger event plan, quota display snapshot, and reconciliation target; `mcp_standard_error_codes` now covers PRD §9.6 error taxonomy, auth/rights/data/limit/system categories, client actions, internal-code mapping, and versioned MCP `error.detail`; `mcp_tool_limiter` now covers per-tool rate-limit metadata, standard/high-cost concurrency pools, ordinary pool protection, budget pre-debit/failure-refund semantics, high-cost planned queue metadata, and future `RATE_LIMITED`/`BUDGET_EXCEEDED` contract; `mcp_compatibility_status` now covers `GET /mcp/compatibility/status`, official Inspector/TypeScript SDK v1.x targets, target-client matrix, status-page source fields, and US-M08 `as_of`/delay/source test vectors; `mcp_developer_console_backend` now covers `POST /mcp/developer-console/plan`, `GET /mcp/runtime` `mcp_developer_console_*` metadata, connection guide, API key/OAuth route metadata, PRD §9.7 scope catalog, quota/request_id fields, request-log schema, initialize/tools-list/tools-call examples, `deploy/mcp/developer-console.contract.json`, `core.mcp_developer_console_request_log` / `governance.mcp_developer_console_contract` schema scaffolds, and `check:mcp-developer-console`; `mcp_developer_console_log_store_smoke` now covers guarded `POST /mcp/developer-console/log-store-smoke`, `deploy/mcp/developer-console-log-store-smoke.contract.json`, `apps/worker/src/mcp-developer-console-log-store-smoke.test.ts`, `scripts/check-mcp-developer-console-log-store-smoke-contract.mjs`, and `check:mcp-developer-console-log-store-smoke`, proving hash-only Hyperdrive insert/read/delete for `core.mcp_developer_console_request_log`; `mcp_target_client_live_e2e_handoff` now covers `deploy/mcp/target-client-live-e2e-handoff.contract.json`, target-client E2E templates/packet intake, `scripts/check-mcp-target-client-live-e2e-packets.mjs`, `scripts/check-mcp-target-client-live-e2e-packet-fixtures.mjs`, `scripts/check-mcp-target-client-live-e2e-handoff.mjs`, and `check:mcp-target-client-live-e2e-*`, allowing hash-only evidence packets for Inspector / TypeScript SDK / Claude Desktop / Cursor / ChatGPT Connector without accepting live evidence yet; live OAuth provider, token storage, live key generation, persistent key hash table, auth middleware, live tool execution, persistent cursor store, live usage ledger read/write, billing reconciliation, live limiter enforcement, durable queue writes, accepted /developer-console frontend UI evidence, production Console log store cutover, live SDK/Inspector smoke, target-client e2e accepted packets, and public status page remain absent | The repo now has a concrete default-deny `/mcp` protocol surface plus testable OAuth/PKCE, API key lifecycle, schema validation, tool versioning, bounded retrieval, usage envelope, standard error, tool limiter, compatibility/status, backend Developer Console contracts, a guarded Console log-store smoke, and a target-client live E2E evidence handoff, but it still cannot authenticate or execute Remote MCP calls live and cannot claim MCP-09 complete until production Console/live ledger/auth surfaces and target-client E2E land | Revisit when external target-client evidence packets and live Console/usage/API-key/OAuth release-gate evidence arrive because Sprint 2.3 now has its local user-visible Console surface but still lacks accepted live target-client packets |
| MCP target-client live E2E transition review | `deploy/mcp/target-client-live-e2e-transition-review.contract.json`, `npm run check:mcp-target-client-live-e2e-transition-review`, and `npm run check:mcp-target-client-live-e2e-transition-review-fixtures` now prove accepted target-client packet alone cannot complete MCP-09; each target client also needs Developer Console UI/live Console log store/live usage ledger/live API key/OAuth/status-page gates to pass | The repo can intake future Inspector/TypeScript SDK/Claude Desktop/Cursor/ChatGPT Connector packets without accidentally completing Sprint 2.3, but current packet directory is empty and all decisions remain blocked | After target-client packets land under `deploy/mcp/target-client-live-e2e-packets/`, rerun `npm run check:mcp-target-client-live-e2e-packets`, `npm run check:mcp-target-client-live-e2e-transition-review`, `npm run check:p0-open-requirement-transition-review`, and `npm run check:sprint-completion-audit` before changing MCP-09 or Sprint 2.3 checkbox state |
| Sprint 2.4 subscription billing, Workflows, alerts, and correction surfaces | `subscription_lifecycle_audit` backend scaffold now covers `@aiphabee/account-runtime` nested runtime capability, `POST /account/subscription/lifecycle/plan`, upgrade/downgrade/renew/cancel/grace-period actions, Free/Plus/Pro/Developer/Team/Enterprise plan codes, billing-state transition planning, no-call billing provider block, `audit.subscription_lifecycle_event` schema scaffold, and `check:subscription-lifecycle`; `usage_billing_reconciliation` backend scaffold now covers `@aiphabee/usage-ledger` nested runtime capability, `POST /usage/billing/reconciliation/plan`, invoice credits vs ledger credits consistency, invoice line -> usage ledger entry -> usage event -> request_id traceability, support request_id investigation, `core.subscription_invoice` / `core.subscription_invoice_line` schema scaffolds, and `check:usage-billing-reconciliation`; `high_cost_usage_reservation` backend scaffold now covers `POST /usage/high-cost/reservation/plan`, `/analytics/high-cost/plan` `usage_reservation` linkage, estimate/confirmation gate/pre-debit/failure-refund plans, request_id/idempotency double-charge guard, `core.usage_credit_reservation` schema scaffold, and `check:high-cost-usage-reservation`; `workflow_task` backend scaffold now covers `POST /agent/workflows/tasks/plan`, `GET /agent/runtime` `workflow_tasks` readiness, task_id/resume/notification plans, `core.workflow_task` / `core.workflow_task_checkpoint` schema scaffolds, and `check:agent-workflow-task`; `deep_report_workflow` backend scaffold now covers `POST /research/reports/deep/plan`, `GET /research/runtime` nested readiness, fetch -> deterministic analysis -> section generation -> citation validation -> evidence index -> rerun seed stages, static report metadata, task_id/Workflow task linkage, `core.deep_report_snapshot` / `core.deep_report_evidence_index` schema scaffolds, and `check:deep-report-workflow`; `watchlist_alerts` backend scaffold now covers `@aiphabee/watchlist-runtime`, `GET /watchlist/runtime`, `POST /watchlist/alerts/plan`, explicit confirmation, independent `alerts.write` scope, idempotency key, price/announcement/metric alert kinds, dedupe, frequency, quiet-period controls, source evidence requirement, planned notification fanout, `core.watchlist` / `core.watchlist_item` / `core.watchlist_alert_rule` / `core.watchlist_alert_event` schema scaffolds, and `check:watchlist-alerts`; `watchlist_briefings` backend scaffold now covers `GET /watchlist/runtime` nested readiness, `POST /watchlist/briefings/plan`, daily/weekly cadence, material-change-only filtering, empty-briefing suppression, source evidence requirement, price/announcement/metric source planning, planned notification fanout, `core.watchlist_briefing` / `core.watchlist_briefing_item` schema scaffolds, and `check:watchlist-briefings`; `data_correction_notifications` backend scaffold now covers `GET /research/runtime` nested readiness, `POST /research/data-corrections/plan`, source_record_id correction matching, saved-run evidence snapshot impact marking, old report immutability, in_app/email planned notification fanout, `core.data_correction_event` / `core.research_run_correction_impact` / `core.user_notification` schema scaffolds, and `check:data-correction-notifications`; `mcp_revocation_enforcement` backend scaffold now covers `GET /mcp/runtime` enforcement readiness, `POST /mcp/revocations/enforce/plan`, `POST /mcp` credential context guard, OAuth connection/API key status normalization, revoked/rotated/unknown `AUTH_REQUIRED` denial before tool execution and usage debit, `core.mcp_credential_revocation` / `governance.mcp_revocation_enforcement_contract` schema scaffolds, and `check:mcp-revocation-enforcement`; `agent_kill_switch` backend scaffold now covers `GET /agent/runtime` `kill_switch` readiness, `POST /agent/kill-switch/plan`, `POST /agent/runs/plan` `degraded_kill_switch`, model/tool switch state normalization, safe degradation metadata, no-tool-call fallback, `core.agent_kill_switch_state` / `governance.agent_kill_switch_contract` schema scaffolds, and `check:agent-kill-switch` | The repo can now prove ACC-03 lifecycle audit semantics, ACC-04 billing-to-usage-ledger traceability, US-W10 high-cost reservation/pre-debit/failure-refund planning, AGT-09 task_id/resume/notification planning, PRD §6.5/RES-04 deep-report Workflow planning, RES-05/US-W09 watchlist alert planning, RES-06 daily/weekly watchlist briefing planning, DAT-08 saved-report correction notification planning, ACC-06 no-live revoke enforcement, and US-O04 no-live kill-switch degradation without writing subscription/audit/invoice/ledger/task/report/watchlist/correction/revocation/kill-switch rows, reading live ledger or flag rows, starting Cloudflare Workflows, executing tools/models, sending queue notifications, calling billing providers, or enabling live MCP auth middleware | Sprint 2.4 backend scaffold is now 10/10. It still cannot claim paid billing, executable deep reports, scheduled watchlist briefing delivery, live correction discovery/fanout, live credential store enforcement, or live kill-switch behavior; the next non-frontend tracker bottleneck is Sprint 3.1 P0 tool/backend catalog closure |
| Sprint 3.1 P0 tool/backend catalog | `get_event_timeline` backend scaffold now covers `@aiphabee/event-timeline`, `POST /tools/get-event-timeline`, tool schema/MCP validation/versioning/pagination/usage/golden fixture integration, synthetic announcement/market/financial-disclosure/corporate-action timeline rows, sourceRecordId and relatedData source links, type filtering, cursor pagination, and standard error mapping; `p0-tool-catalog-consistency` now aligns all 16 PRD §9.2 P0 tools across Shared Tool Registry, tool schemas, MCP schema validation/versioning/pagination/usage, Agent enforcement, Evidence/Lineage mapping, and golden fixtures; `run_event_study` backend scaffold now covers `@aiphabee/analytics-tools`, `POST /analytics/event-study`, `GET /analytics/runtime` capability metadata, event_date/event_window/benchmark/abnormal_return_method output, explicit `missing_observations`, deterministic summary metrics, event-study contract checks, and high-cost analytics 20-50 credits independent-pool planning; `localized_response_contract` now covers `@aiphabee/agent-runtime` `response_presentation`, `POST /agent/runs/plan` locale/depth parsing, zh-Hant/zh-Hans/en invariants, newbie/professional depth invariants, and bilingual financial terminology glossary; `authorized_session_memory` now covers `@aiphabee/account-runtime`, `POST /account/authorized-memory/plan`, user-visible view/upsert/delete no-write planning, allowed preference/consent keys, forbidden prompt/answer/financial/credential payload classes, and empty schema scaffolds; `security_history` now covers `@aiphabee/security-tools`, `POST /tools/get-security-history`, required `as_of`, historical name/industry/index constituent membership fixtures, no-latest fallback policy, and empty schema scaffolds; `restricted_exports` now covers `@aiphabee/data-access-gateway`, `POST /gateway/exports/plan`, `exports.read` high-risk scope, `channel=export` field authorization, row/time bounds, watermark metadata, and empty export request/audit scaffolds; live event rows, live MCP execution, partner rows, queue writes, usage debits, generated translation, live memory/history writes/reads, live export artifact writes, and frontend timeline/event-study/localization/memory/history/export UI remain absent | The repo can now prove the full P0 tool catalog consistency with `check:p0-tool-catalog` and `test:golden`, no-live event-study missing-sample behavior with `check:event-study`, language/depth/terminology invariants with `check:localized-response`, authorized-only memory controls with `check:authorized-session-memory`, point-in-time security history controls with `check:security-history`, and restricted export controls with `check:restricted-exports` while preserving no-live/no-frontend posture | Sprint 3.1 backend scaffold is now 9/9; revisit when Claude returns delegated frontend surfaces or when continuing Sprint 3.2 public docs/status/privacy/share/package surfaces |
| Sprint 3.2 public operations and package formalization | `package_pricing` backend scaffold now covers `@aiphabee/account-runtime`, `GET /account/runtime` nested readiness, `GET /account/package-pricing`, PRD §15.2 Pro `HK$228/month` and Developer `HK$688+/month` validation prices, Pro/Developer entitlement catalog, quota contract references, Developer no-write overage planning, `core.plan_pricing_catalog` / `core.plan_entitlement_bundle` / `governance.package_pricing_contract` schema scaffolds, and `check:package-pricing`; `public_status_docs` backend scaffold now covers `@aiphabee/public-ops`, `GET /public/runtime`, `GET /public/status`, `GET /public/docs`, Worker API/Remote MCP/Data Gateway/usage billing/public docs status components, local API/MCP/privacy/terms publication drafts, required-section validation, `core.public_status_component` / `core.public_document_publication` / `governance.public_operations_contract` schema scaffolds, and `check:public-ops`; `support_request_id_investigation` backend scaffold now covers `@aiphabee/support-ops`, `GET /support/runtime`, `GET /support/help-center`, `POST /support/request-id-investigation/plan`, help center topics, request_id metadata-only investigation, allowed support lookup fields, forbidden sensitive fields, `core.support_ticket` / `audit.support_investigation_event` / `governance.support_request_id_contract` schema scaffolds, and `check:support-ops`; `private_share_link` backend scaffold now covers `@aiphabee/sharing-runtime`, `GET /sharing/runtime`, `POST /sharing/private-links/plan`, creator/recipient `exports.read` checks, Data Gateway recipient entitlement recheck, effective field intersection, recipient redaction, no-expansion flags, expiry/watermark policy, `core.private_share_link` / `audit.private_share_event` / `governance.private_sharing_contract` schema scaffolds, and `check:private-sharing`; `static_report_artifact` backend scaffold now covers `@aiphabee/research-runtime`, `POST /research/reports/static/plan`, `GET /research/runtime` `static_report_artifact`, allowed-scope `exports.read`, source run/workspace context, html/pdf/image formats, generated time, data delay, data/methodology/rights versions, disclaimer, watermark metadata, `core.static_report_artifact` / `audit.static_report_event` / `governance.static_report_contract` schema scaffolds, and `check:static-report`; `partner_reconciliation_report` backend scaffold now covers `@aiphabee/usage-ledger`, `POST /usage/partner-reconciliation/plan`, `GET /usage/runtime` `partner_reconciliation_report`, dataset/channel/package_code/user_id grouping, usage_count/credits/metered_rows aggregation, request_id and usage_event_id traceability, daily/weekly SLA counters, sensitive-payload exclusions, `core.partner_reconciliation_report` / `core.partner_reconciliation_report_line` / `audit.partner_reconciliation_event` / `governance.partner_reconciliation_contract` schema scaffolds, and `check:partner-reconciliation-report`; `partner_sla_reconciliation_readiness` backend readiness now covers `GET /usage/partner-sla/reconciliation-readiness`, `GET /usage/runtime` `partner_sla_reconciliation_readiness`, daily/weekly report aggregation, delay/missing/error/backfill SLA summary, request_id -> usage_event trace completeness, partner support release-gate composition, sensitive-payload exclusion, `deploy/usage/partner-sla-reconciliation-readiness.contract.json`, governance/task notes, and `check:partner-sla-reconciliation-readiness`; `field_authorization_config` backend scaffold now covers `@aiphabee/data-access-gateway`, `POST /gateway/field-authorizations/changes/plan`, `GET /gateway/runtime` `field_entitlement_enforcement.operations_config`, approval status, policy version, effective time, future `core.data_entitlement` / `core.workspace_entitlement` row effects, `core.field_authorization_change` / `audit.field_authorization_approval` / `governance.field_authorization_config_contract` schema scaffolds, and `check:field-authorization-config`; `account_data_request` backend scaffold now covers `@aiphabee/account-runtime`, `POST /account/data-requests/plan`, `GET /account/runtime` `data_requests`, download/delete request actions, retention_policy_version, secure JSON delivery planning, deletion erasure-vs-retain planning, subscription_billing/usage_ledger/audit_log retention holds, unsupported scope blocking, `core.account_data_request` / `core.account_data_request_item` / `audit.account_data_request_event` / `governance.account_data_request_contract` schema scaffolds, and `check:account-data-request`; WCAG work, live billing provider, final commercial quote, live pricing writes, live incident feed, final legal approval, live ticket/log/billing-provider reads, live share handle generation, live report artifact writes, live partner report export/artifact writes, live partner SLA artifact store/portal/settlement, live policy activation/writes, live account data export/erasure/delivery, and frontend pricing/status/docs/help/share/report/ops/account privacy UI remain absent | The repo can now prove Pro/Developer package assumptions, entitlement boundaries, quota references, overage no-write posture, redistribution limits, public status component coverage, API/MCP/privacy/terms draft coverage, required-section checks, help center topic coverage, request_id investigation metadata boundaries, sensitive-field default denial, private sharing no-expansion policy, recipient entitlement recheck, static report required metadata, partner reconciliation grouping by dataset/channel/package/user/usage, DAT-10 daily/weekly SLA readiness aggregation, field authorization approval/version/effective-time planning, account data download/delete request retention controls, no provider calls, no SQL, and no persistent writes while preserving delegated frontend boundaries | Sprint 3.2 backend/contract scaffold is now 8/9; the only remaining Sprint 3.2 tracker item is WCAG frontend work delegated to Claude, while Codex-owned Sprint 3.3 backend release-gate scaffolding is now closed |
| Sprint 3.3 release gate scaffolding | `p0_rights_matrix_coverage` backend scaffold now covers `@aiphabee/data-access-gateway`, `GET /gateway/rights-matrix/p0/coverage`, `GET /gateway/runtime` `p0_rights_matrix_coverage`, Tool Registry 16 P0 tool coverage, 9 P0 dataset/field groups, Web/MCP/export/enterprise configured_default_deny slots, `core.p0_rights_matrix_entry` / `governance.p0_rights_matrix_contract` schema scaffolds, and `check:p0-rights-matrix-coverage`; `data_coverage_release_gate` backend scaffold now covers `GET /gateway/data-coverage/release-gate`, `GET /gateway/runtime` `data_coverage_release_gate`, realtime/delayed/EOD freshness markers with required labels, corporate_actions/financial_restatements/delistings/identifier_history coverage domains, `core.data_coverage_release_gate` / `governance.data_coverage_release_gate_contract` schema scaffolds, and `check:data-coverage-release-gate`; `golden_correction_rollback_drill` backend scaffold now covers `@aiphabee/research-runtime`, `POST /research/golden-correction-rollback-drill/plan`, `GET /research/runtime` nested readiness, `npm run test:golden` linkage to 8 quality samples / 12 quality rules / 16 tool fixtures, saved run -> data correction notification -> rollback replay planning, old report immutability, `core.golden_correction_rollback_drill` / `governance.golden_correction_rollback_drill_contract` schema scaffolds, and `check:golden-correction-rollback-drill`; `product_agent_release_gate` backend scaffold now covers `@aiphabee/agent-runtime`, `POST /agent/release-gates/product-agent/plan`, `GET /agent/runtime` `product_agent_release_gate`, pre-tool-call `ABC` ambiguity blocking, silent-selection denial, tool-result/deterministic-calculation numeric source binding, answer/evidence unsourced-number blocking, `core.product_agent_release_gate` / `governance.product_agent_release_gate_contract` schema scaffolds, and `check:product-agent-release-gate`; `agent_label_budget_release_gate` backend scaffold now covers `POST /agent/release-gates/label-budget/plan`, `GET /agent/runtime` `agent_label_budget_release_gate`, fact/calculation/inference/unknown binding checks, categorical evidence strength, high-cost budget estimate, confirmation-before-enqueue, independent high-cost pool routing, usage reservation pre-debit/failure-refund/idempotency no-write semantics, `core.agent_label_budget_release_gate` / `governance.agent_label_budget_release_gate_contract` schema scaffolds, and `check:label-budget-release-gate`; `task_replay_mode_release_gate` backend scaffold now covers `POST /agent/release-gates/task-replay-mode/plan`, `GET /agent/runtime` `task_replay_mode_release_gate`, long-task `task_id`/`resume_handle`/checkpoint semantics, saved-report deterministic replay seed, old report immutable/no-silent-rewrite replay planning, newbie/professional response-depth data/evidence/methodology invariants, `core.task_replay_mode_release_gate` / `governance.task_replay_mode_release_gate_contract` schema scaffolds, and `check:task-replay-mode-release-gate`; `mcp_protocol_release_gate` backend scaffold now covers `@aiphabee/mcp-runtime`, `POST /mcp/release-gates/protocol/plan`, `GET /mcp/runtime` `mcp_protocol_release_gate_*` readiness, Streamable HTTP initialize, trusted/untrusted Origin probes, revoked credential `AUTH_REQUIRED` pre-execution denial, rights default-deny, `get_quote_snapshot` input/output schema metadata, compatibility vectors, `core.mcp_protocol_release_gate` / `governance.mcp_protocol_release_gate_contract` schema scaffolds, and `check:mcp-protocol-release-gate`; `mcp_auth_limits_release_gate` backend scaffold now covers `@aiphabee/mcp-runtime`, `POST /mcp/release-gates/auth-limits/plan`, `GET /mcp/runtime` `mcp_auth_limits_release_gate_*` readiness, OAuth `S256` PKCE and revocable scopes, OAuth revoke future-call denial, API key old-key rotation denial, API key revoke denial, `get_price_history` opaque request-bound cursor metadata, row/time-window bypass rejection, stable MCP error mappings, `core.mcp_auth_limits_release_gate` / `governance.mcp_auth_limits_release_gate_contract` schema scaffolds, and `check:mcp-auth-limits-release-gate`; `mcp_target_clients_console_release_gate` backend scaffold now covers `POST /mcp/release-gates/target-clients-console/plan`, `GET /mcp/runtime` `mcp_target_clients_console_release_gate_*` readiness, Inspector/TypeScript SDK/Claude Desktop/Cursor/ChatGPT Connector target matrix, 10-minute first-call target, `docs/public/mcp.md` guide artifact, Console request_id/scope/credential/usage/error/provenance reconciliation fields, secret/sensitive field exclusions, `core.mcp_target_clients_console_release_gate` / `governance.mcp_target_clients_console_release_gate_contract` schema scaffolds, and `check:mcp-target-clients-console-release-gate`; `prompt_injection_tool_denial_release_gate` backend scaffold now covers `@aiphabee/agent-runtime`, `POST /agent/release-gates/prompt-injection/plan`, `GET /agent/runtime` `prompt_injection_tool_denial_release_gate`, document sanitizer malicious fixture isolation, document-origin tool instruction non-execution, `sql.query` / `http.fetch` / `admin.override` pre-execution denial, registered baseline tool schema/read-only/no-SQL/no-URL enforcement, `core.prompt_injection_tool_denial_release_gate` / `governance.prompt_injection_tool_denial_release_gate_contract` schema scaffolds, and `check:prompt-injection-tool-denial-release-gate`; `privacy_share_release_gate` backend scaffold now covers `@aiphabee/sharing-runtime`, `POST /sharing/release-gates/privacy-share/plan`, `GET /sharing/runtime` `privacy_share_release_gate`, personal-data download secure scoped no-write delivery, personal-data erasure retention holds, private share recipient entitlement recheck, effective field intersection, redaction/no rights expansion, expiry/watermark/no-public-index, `core.privacy_share_release_gate` / `governance.privacy_share_release_gate_contract` schema scaffolds, and `check:privacy-share-release-gate`; `compliance_ops_release_gate` backend scaffold now covers `@aiphabee/public-ops`, `POST /public/release-gates/compliance-ops/plan`, `GET /public/runtime` `compliance_ops_release_gate`, Type 4 research-boundary copy review, forbidden advice-claim absence, kill-switch safe degradation, support request_id incident trace, observability `run.audit` export required fields/sensitive exclusion, public status incident disclosure surface, `core.compliance_ops_release_gate` / `audit.compliance_ops_drill_event` / `governance.compliance_ops_release_gate_contract` schema scaffolds, and `check:compliance-ops-release-gate`; `billing_rules_release_gate` backend scaffold now covers `@aiphabee/usage-ledger`, `POST /usage/release-gates/billing-rules/plan`, `GET /usage/runtime` `billing_rules_release_gate`, Pro/Developer credits, Developer overage, weighted credits, refund/proration provider-preview block, invoice credits matching ledger credits, request_id -> usage_event -> ledger_entry -> invoice_line traceability, high-cost pre-debit/failure-refund planning, `core.billing_rules_release_gate` / `audit.billing_rules_drill_event` / `governance.billing_rules_release_gate_contract` schema scaffolds, and `check:billing-rules-release-gate`; `partner_support_release_gate` backend scaffold now covers `@aiphabee/usage-ledger`, `POST /usage/release-gates/partner-support/plan`, `GET /usage/runtime` `partner_support_release_gate`, partner reconciliation report generation, request_id -> usage_event trace for partner report rows, metadata-only support investigation by target request_id, SLA delay/missing/error/backfill counters, sensitive-payload exclusion, `core.partner_support_release_gate` / `audit.partner_support_drill_event` / `governance.partner_support_release_gate_contract` schema scaffolds, and `check:partner-support-release-gate`; `publication_economics_release_gate` backend scaffold now covers `@aiphabee/public-ops`, `POST /public/release-gates/publication-economics/plan`, `GET /public/runtime` `publication_economics_release_gate`, public status page scaffold publication, API/MCP/privacy/terms docs manifest, support help-center manifest, Pro/Developer package pricing catalog, PRD §15.5 expected-usage contribution-margin checks, `core.publication_economics_release_gate` / `audit.publication_economics_drill_event` / `governance.publication_economics_release_gate_contract` schema scaffolds, and `check:publication-economics-release-gate`; `performance_availability_release_gate` backend scaffold now covers `@aiphabee/observability`, `POST /observability/release-gates/performance-availability/plan`, `GET /observability/runtime` `performance_availability_release_gate`, PRD §12.1 core API monthly availability, MCP hot/cold P95, Web first-token P95, simple research P95, MCP tool success-rate excluding user/auth errors, route coverage for `/health` / `/mcp` / `/agent/runs/stream` / `/agent/runs/plan`, `core.performance_availability_release_gate` / `audit.performance_slo_drill_event` / `governance.performance_availability_release_gate_contract` schema scaffolds, and `check:performance-availability-release-gate`; `load_dr_incident_drill_release_gate` backend scaffold now covers `@aiphabee/observability`, `POST /observability/release-gates/load-dr-incident-drill/plan`, `GET /observability/runtime` `load_dr_incident_drill_release_gate`, PRD §12.1 peak load, error-rate, DR RTO/RPO targets, synthetic drill report coverage for load test, database restore, worker failover, rollback, incident response, and status communications, `core.load_dr_incident_drill_release_gate` / `audit.load_dr_incident_drill_event` / `governance.load_dr_incident_drill_release_gate_contract` schema scaffolds, and `check:load-dr-incident-drill-release-gate`; partner-signed rights matrix, live entitlement activation, external commercial/legal signoff, live partner rows, live freshness policy, production partner golden corpus, live correction writes, Queue fanout, live rollback execution, live evidence writes, generated-answer label parser, frontend clarification/confirmation UI, live target-client e2e, Developer Console UI, live Console log store, live usage ledger reads, live privacy delivery jobs, live retention policy source, live share handle generation, external privacy/legal signoff, external compliance/legal signoff, live kill-switch flag source, live incident feed, live audit export store, live public status/help deployment, final privacy/terms legal approval, live pricing provider, finance unit-economics signoff, live APM/probe/SLO stores, live load-test artifacts, live DR restore evidence, live failover execution, live incident/status drill artifacts, ops-SRE-product signoff, and frontend release surfaces remain absent | The repo can now prove P0 rights matrix coverage slots, explicit data freshness/coverage release-gate metadata, current synthetic golden fixture pass state, the no-write data-correction rollback drill, planning-time product/Agent ambiguity plus numeric-evidence and post-generation validation release checks, planning-time claim-label/high-cost budget confirmation checks, planning-time long-task resume/saved-report replay/mode-invariant release checks, local MCP Streamable HTTP/Origin/auth/schema release gate checks, local MCP OAuth/API-key/cursor/limit/error-stability release gate checks, local target-client/Developer Console reconciliation contract checks, local prompt-injection sanitizer/isolation plus arbitrary SQL/URL/unregistered tool pre-execution denial release gate checks, and local privacy/share personal-data retention plus private-share no-expansion release gate checks, and compliance/ops research-boundary, kill-switch, incident-response, audit-export release gate checks, and public status/help/privacy/terms publication plus positive Pro/Developer expected-usage unit economics, synthetic performance/availability SLO observations, and synthetic load/DR/incident drill reports without claiming signed external rights evidence, live partner coverage, production corpus signoff, executable rollback, live evidence writes, live label parsing, live queue execution, live OAuth provider, live API key generation, live limiter windows, live usage ledger writes, live target-client e2e, live Console UI/log store, live prompt-injection red-team harness, live tool execution proxy enforcement, live privacy delivery jobs, live retention policy source, live share handle generation, external privacy/legal signoff, external compliance/legal signoff, live kill-switch flag source, live incident feed, live audit export store, or frontend release UI | Sprint 3.3 backend/contract scaffold is now 17/17; remaining release blockers are live artifacts/signoffs rather than missing local release-gate scaffolds |
| P0 open requirement blocker audit | `deploy/governance/p0-open-requirement-audit.contract.json` and `npm run check:p0-open-requirement-audit` now guard the remaining open P0 traceability rows AGT-01 / AGT-07 / MCP-09 while keeping STK-07 classified as P1 backlog; it now links both the P0 requirement evidence handoff and the Claude-owned frontend release evidence handoff, but this does not implement frontend Ask progress, clickable evidence cards, Developer Console UI, production Console log cutover, live target-client E2E, or live model/user ToolLoop evidence | The repo can prevent accidental completion drift for the remaining P0 rows and can point future handoffs at the exact missing evidence, but it still cannot claim those requirements or any Sprint exit gate complete | When any AGT-01 / AGT-07 / MCP-09 live, frontend, or target-client evidence lands, update `deploy/governance/p0-open-requirement-audit.contract.json`, rerun `npm run check:p0-open-requirement-audit`, then rerun `npm run check:p0-open-requirement-transition-review`, `npm run check:traceability-matrix`, and `npm run check:sprint-completion-audit` before changing §M checkboxes |
| P0 open requirement evidence handoff | `deploy/governance/p0-open-requirement-evidence-handoff.contract.json`, `deploy/governance/p0-open-requirement-evidence-templates/`, `deploy/governance/p0-open-requirement-evidence-packets/`, `npm run check:p0-open-requirement-evidence-packets`, `npm run check:p0-open-requirement-evidence-packet-fixtures`, and `npm run check:p0-open-requirement-evidence-handoff` now define the redacted hash-only intake path for AGT-01 / AGT-07 / MCP-09 evidence | The repo can accept future Claude/frontend/live packets without raw secrets, prompts, responses, Console payloads, account IDs, workspace IDs, or connection strings, but no accepted packets exist yet and no P0 row can move to complete | When a packet is copied from the template into `deploy/governance/p0-open-requirement-evidence-packets/` with `status=accepted`, rerun all three `npm run check:p0-open-requirement-evidence-*` commands plus `npm run check:p0-open-requirement-audit`, then update §M only if the requirement-specific release gate also turns green |
| P0 open requirement transition review | `deploy/governance/p0-open-requirement-transition-review.contract.json`, `npm run check:p0-open-requirement-transition-review`, and `npm run check:p0-open-requirement-transition-review-fixtures` now prove accepted packet alone cannot complete AGT-01 / AGT-07 / MCP-09; each row must also pass its linked live/frontend/target-client release gates and the matching Claude-owned frontend release evidence packet (`agent_ask_progress_ui`, `agent_evidence_card_ui`, or `developer_console_ui`) | Future packet intake has a machine-readable transition guard; AGT-01 / AGT-07 / MCP-09 now have accepted frontend packets, but current decisions remain blocked because accepted P0 requirement packets are still absent and linked release gates are still false | After any accepted P0 packet or linked release gate lands, rerun `npm run check:p0-open-requirement-transition-review`, `npm run check:frontend-release-evidence-packets`, and the requirement-specific release gate; only if the matching decision is allowed should §M, `deploy/governance/p0-open-requirement-audit.contract.json`, and Sprint exit gates be updated |
| Frontend release evidence handoff | `deploy/governance/frontend-release-evidence-handoff.contract.json`, `deploy/governance/frontend-release-evidence-templates/`, `deploy/governance/frontend-release-evidence-packets/`, `npm run check:frontend-release-evidence-packets`, `npm run check:frontend-release-evidence-packet-fixtures`, and `npm run check:frontend-release-evidence-handoff` now define the redacted hash-only intake path for frontend evidence covering Ask progress UI, evidence cards, comparison/screening UI, research library UI, Developer Console UI, and WCAG 2.1 AA evidence; AGT-01 / AGT-07 / MCP-09 transition review now consumes the matching frontend packets before allowing §M completion | Sprint completion audit and P0 transition review can now track frontend completion as a real blocker manifest instead of relying on chat history or branch names; Ask progress UI, evidence cards, comparison/screening UI, research library UI, and Developer Console UI evidence are accepted, but frontend release surfaces remain incomplete because WCAG 2.1 AA packet is still missing | When remaining frontend evidence is ready, copy the matching template into `deploy/governance/frontend-release-evidence-packets/`, keep raw screenshots/DOM/HTML/prompts/responses/secrets out of the packet, then rerun `npm run check:frontend-release-evidence-packets`, `npm run check:frontend-release-evidence-packet-fixtures`, `npm run check:frontend-release-evidence-handoff`, `npm run check:p0-open-requirement-transition-review`, and `npm run check:sprint-completion-audit` before changing Sprint or §M checkbox state |
| External tracker sync for P0 traceability | Repo-local `AIP-P0-*` issue refs now exist, but no external tracker has been selected | Release governance can use the repo ledger, but GitHub/Jira/Linear dashboards will not show the work automatically | External tracker is selected or the team requests import/sync |
| Sprint completion audit drift guard | `deploy/governance/sprint-completion-audit.contract.json` and `npm run check:sprint-completion-audit` now prove the current open Sprint state, blocker manifests, and tracker ledger stay aligned, but the audit is intentionally blocked from claiming completion | The repo can prevent accidental exit-gate drift while external/live/frontend blockers remain open, but cannot replace signed evidence, live smoke packets, target-client E2E packets, or Claude-owned frontend completion | Any accepted evidence packet, frontend completion handoff, live credential smoke, or Sprint table change occurs, then update the audit contract and rerun `npm run check:sprint-completion-audit` plus the linked manifest checker |
| Mainline publication of integrated backend/frontend work | `origin/feat/web-frontend` is already an ancestor of `main`, PR #1 / PR #2 are recorded as merged, and `deploy/governance/mainline-publication-readiness.contract.json` plus `npm run check:mainline-publication-readiness` now prove the frontend merge state is clean without requiring another Claude branch merge | Local `main` is not behind `origin/main`, but publication still needs the chosen push/review path rather than another merge; this repo-local gate does not execute push or prove remote CI after publication | Push or open the review path for current `main` after the team explicitly approves publication, then rerun `npm run check:mainline-publication-readiness` and the remote CI gate |
