# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: (migration)
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| External Gate 0 approvals: rights matrix, HKEX/vendor role, Type 4 written opinion, commercial terms, signatures | Requires data partner, legal/compliance, business, and executive evidence outside this repo | Phase 1 market-data and MCP surfaces remain blocked; docs-only packet uses `DEFAULT_DENY` | Signed Gate 0 evidence arrives or leadership records a signed waiver/fallback |
| Partner-signed data contract and real source samples | Current data methodology is a baseline, not a signed contract | Physical schema and ingestion must avoid assuming partner field names/SLAs | Partner provides signed contract, field dictionary, source IDs, delivery samples, and SLA |
| Production partner golden corpus and cost-reviewed quality/commercial gate | Synthetic v0 fixtures and deterministic quality rules now pass locally, but they are not partner-approved source samples and do not meet PRD §10.7 production volume | `DATA_QUALITY_HOLD` is executable in the fixture gate, but production data correctness, rights coverage, and unit economics remain unproven | Partner sample corpus, source hashes, data-owner sign-off, and real cost review arrive |
| Frontend runtime scaffold: TanStack Start + Vite + design-system baseline | User delegated frontend follow-up to Claude during `engineering-runtime-scaffold` | Sprint 0.4 frontend and design-system leaves remain open; current Codex scaffold intentionally has no `apps/web` | Claude delivers frontend scaffold or asks Codex to integrate it |
| Engineering runtime remaining surfaces: model provider live execution smoke, Cloudflare resource provisioning/smoke, live Hyperdrive `SELECT 1`, OTLP live export + persistent eval write/read smoke, provider secret store provisioning/rotation smoke | Non-frontend scaffold now covers npm workspaces, Hono Worker health, shared data contracts, CI, env contract, env validation, Cloudflare binding contract, AI SDK dry-run Agent Runtime skeleton, model provider/streaming guard contract, local run.audit/run.eval telemetry events, eval-store/OTLP destination contract guard, Supabase-compatible migration tooling, and provider secret-store contracts/runbooks | Phase 0 can run a Worker locally and validate dry-run agent policy, model-provider guard, local telemetry, eval-store destination guard, DB migration contracts, and secret-store contracts, but cannot claim staging deployability, live data access, real model execution, live telemetry export, or managed secret operations | Start a focused backend/runtime slice after frontend ownership is settled |
| Sprint 1.1 live data surfaces: partner-loaded securities master/raw snapshots, financial facts, corporate actions, real Serving Gateway, field entitlements, usage ledger, account/workspace model | Data Access Gateway default-deny, quality-hold, synthetic field-entitlement guards, entitlement DB policy-source compiler, synthetic financial restatement cases, synthetic corporate-action adjustment cases, Serving read planner, Serving quality release isolation planner, Serving query planner, Serving SQL descriptor/text compiler, Serving execution adapter, Serving result envelope, and Usage Ledger event writer planner are executable; security master/raw snapshot, financial fact/restatement, corporate-action/adjustment, account/workspace entitlement, usage ledger, and Serving Store schemas now exist, but no partner data, live Serving SQL execution/rows/writes, live DB entitlement reads, live usage writes, or billing reconciliation are wired | Gateway can reject unsafe access, compile entitlement row snapshots, plan blocked/held reads, map quality state to Serving release posture, plan released snapshot query material, plan SQL descriptor bindings/text, defer execution with empty rows, expose a no-live result envelope, and plan per-call usage events, but cannot return real licensed data or persist usage accounting | Signed data contract and first physical seed/apply slice is ready |
| Sprint 1.2 tool execution and evidence surfaces: registered tool handlers, precise schemas, golden fixtures, MCP/API endpoints, Evidence/Lineage service | Shared Tool Registry scaffold now centralizes 9 read-only tools, all 9 have no-live synthetic handlers, local input/output schema contracts exist, tool golden fixtures run in `test:golden`, and the no-write Evidence/Lineage service planner/schema are scaffolded; MCP protocol integration, runtime schema serving, live route replay, live DB writes, and live partner rows remain absent | Agent runtime can reject unregistered tools; `/tools/runtime` reports registry metadata; tool routes return synthetic data/scope/lineage; `/evidence/runtime` reports schema-ready no-write service capability; `/evidence/records/plan` links tool calls to source records/data versions/methodologies/user-visible citations without SQL or DB writes; `check:tool-schemas` validates schema IDs/envelope/error enum; `test:golden` validates 8 quality samples and 9 tool response fixtures | Revisit when MCP protocol endpoint/runtime schema serving/live route replay starts, or when partner source records and live DB writes are authorized |
| Sprint 1.3 Agent runtime surfaces: context, ToolLoopAgent, streaming progress, evidence binding, answer structure, failure recovery, model routing | Agent run context, no-model ToolLoopAgent planner, pre-tool-call resolution, budget/stop policy, tool enforcement, numeric source guard, answer/evidence contract, failure recovery policy, and model routing audit scaffolds now cover run/user/workspace, subscription plan, synthetic default-deny entitlements, registry version, registered/versioned/schema-bound/permission-aware tool checks, no arbitrary SQL/URL, tool versions/schema/scope, budget dimensions, dry-run model tier, phased tool planning, max-3 parallel read-only chunks, public progress event contracts, stop/retry policy, security/time/currency/methodology preflight, assumptions, blocking clarification for critical security ambiguity, deterministic budget estimates, `stopped_budget`, partial graceful stop response, continuation cost, two-consecutive same-class error retry stop, no-source concrete financial number blocking, PRD 8.3 ordered answer sections, fact/calculation/inference/unknown claim labels, evidence strength, evidence-card payload fields, failed-tool-call-only retry, completed evidence reuse, partial answer fallback, usage-ledger idempotency placeholder, no-double-charge retry billing, lightweight/main/deterministic-code model routing tiers, Cloudflare AI Gateway planned provider linkage, fallback triggers, model-change audit fields, safe-cache, and sensitive audit redaction; no actual tool execution, live streaming transport, live model execution, post-generation evidence binding, live ledger writes, live token/cost/fallback log writes, durable run-state persistence, or frontend Ask/evidence-card rendering exists | `/agent/runtime` reports run-context, ToolLoopAgent planner, preflight, budget-stop, tool-enforcement, numeric-source guard, answer-evidence, failure-recovery, and model-routing-audit readiness; `/agent/runs/dry-run` returns complete no-model context and rejects unregistered SQL/URL tools; `/agent/runs/preflight` resolves or clarifies context; `/agent/runs/plan` returns phased planned_no_model or stopped_budget steps with embedded preflight, budget_stop_policy, tool_enforcement, numeric_source_guard, answer_evidence_contract, failure_recovery_policy, and model_routing_audit; `check:agent-run-context`, `check:tool-loop-agent`, `check:pre-tool-call-resolution`, `check:budget-stop-policy`, `check:tool-enforcement`, `check:numeric-source-guard`, `check:answer-evidence-contract`, `check:failure-recovery-policy`, and `check:model-routing-audit` guard the shapes | Revisit when actual tool execution/live streaming/live model provider execution starts, or when frontend integration returns from the delegated Claude track |
| Sprint 1.4 workbench/account/eval surfaces | Eval v1 + WVRO backend scaffold now covers `run.eval.eval_v1`, `GET /observability/runtime` eval capability, `POST /observability/eval-v1/plan`, fact/calculation/citation/correct-refusal metrics, unsourced numeric target `<0.1%`, and WVRO four criteria/high-intent actions; internal account/session/manual-plan scaffold now covers `@aiphabee/account-runtime`, `GET /account/runtime`, `POST /account/session/plan`, supported login method contract, session/device revoke plans, manual plan assignment plans, forbidden credential payload classes, and account/workspace/subscription table linkage; usage quota display backend scaffold now covers `GET /usage/runtime`, `POST /usage/quota/plan`, Web Agent/MCP channels, request_id, plan_code, period, credit limit, used/pending/remaining credits, and 5-minute freshness target; stock workbench aggregate scaffold now covers `GET /workbench/runtime`, `POST /workbench/stock/snapshot`, `POST /workbench/stock/announcements`, security profile, quote snapshot, price history, financial facts, derived metrics, announcement search, corporate actions, data-quality statuses, evidence summary, and ambiguous security blocking; STK-04 profitability metrics are computed deterministically and valuation multiples are explicitly blocked with `market_cap_unavailable` when market cap/share-count authority is absent; STK-06 basic announcement search returns source locators without live original document fetch; frontend rendering, live identity provider, cookie issuance, DB writes, billing provider integration, live usage ledger reads, billing reconciliation, persistent eval writes, live OTLP export, full Phase 2 announcement/document tools, and production partner-approved eval corpus remain absent | The repo can now deterministically calculate eval v1/WVRO eligibility, plan internal account/session/manual-plan changes, compute quota display snapshots, and aggregate all six Sprint 1.4 stock workbench backend sections without live reads/writes or frontend dashboards, but cannot claim an internal user can complete end-to-end research until frontend integration and live dependencies land | Revisit when Claude returns frontend workbench/account/quota surfaces for integration, or when Phase 2 full announcement/document search tools start |
| Sprint 2.1 comparison and deterministic analytics | `compare_securities` backend scaffold now covers `@aiphabee/analytics-tools`, `GET /analytics/runtime`, `POST /analytics/compare-securities`, 2-5 security input, security resolution, profile/quote/financial facts fanout, base currency/unit, missing metrics, quality flags, source record IDs, and explicit incomparable reasons; `screen_securities` backend scaffold now covers deterministic natural-language parsing, editable structured conditions, field/operator/value/time-basis/missing-value/source-tool display, confirmation-before-live-execution semantics, synthetic preview hits, `why` explanations, rejected-row reasons, explainable ranking, and a point-in-time future-classification guard with `classification_as_of <= as_of`; `get_financial_ratios` backend scaffold now covers deterministic accounting ratios, formula version, source record IDs, blocked reasons, and synthetic percentile metadata; `calculate_returns_risk` backend scaffold now covers deterministic return/risk metrics, explicit-benchmark Beta, formula version, golden tolerance, source record IDs, and window metadata; `compare_percentiles` backend scaffold now covers peer/index/history percentile comparison, benchmark_as_of, constituent_as_of, point-in-time policy, percentile rank, and source record IDs; `plan_high_cost_analytics` backend scaffold now covers screen/compare PRD credit-weight estimates, `analytics_high_cost` independent pool routing, ordinary pool protection, confirmation, pre-debit/failure-refund metadata, idempotency, and no-write enqueue planning; frontend comparison/screening/ratios/returns-risk/percentile UI, MCP registration, live FX conversion, live historical constituents/industry/names, live peer/benchmark constituents, durable queue writes, live usage-ledger debits/refunds, and live data remain absent | The repo can now compare a small synthetic security set, preview editable screens with explanations, block future classification metadata for historical screens, compute deterministic ratio rows, compute deterministic return/risk rows, compare synthetic peer/index/history percentiles, and plan high-cost analytics onto a separate pool without live writes, but it still cannot provide the delegated UI workflows or live data execution | Revisit when Claude returns the comparison/screening Web UI, or when live queue/usage-ledger/MCP limiter work starts in later sprints |
| Sprint 2.2 announcement and research surfaces | `search_announcements` backend scaffold now covers `@aiphabee/document-tools`, `GET /documents/runtime`, `POST /documents/search-announcements`, company/security resolution, published_at range, category, keyword, language, limit filters, title/date/category/language/summary result rows, `document_id`, `source_record_id`, page/anchor synthetic locators, untrusted document policy metadata, and ambiguous security blocking; `get_announcement` backend scaffold now covers `POST /documents/get-announcement`, `document_id` lookup, optional sections, bounded authorized excerpts, page/paragraph/source_record synthetic locators, and no-fabrication for missing documents/sections; `document_sanitizer` now covers announcement-excerpt script/hidden text/suspicious instruction removal with audit metadata and document-origin tool invocation disabled; `search_documents` now covers synthetic pgvector-style chunk ranking, metadata filter pushdown, sanitized snippets, similarity scores, and page/paragraph/source locators; `diff_announcements` now covers FY2023/FY2024 synthetic annual-results comparison, revenue/operating_profit schema-bound extraction, `announcement_numeric_extraction_v0` validation, base/comparison absolute/percent change, and page/paragraph/source locators for every extracted value and diff side; `save_research_run` now covers question/tool input/evidence/model/prompt/parameter snapshots, immutable `snapshot_id`, replay seed, no-write persistence plan, and old-report mutation disabled metadata; `replay_research_run` now covers saved/current run no-write replay planning, data/model/parameters diff classification, preserved old snapshot IDs, and silent rewrite blocking; live pgvector index/SQL execution, Vectorize, original document fetch, full document return, webpage/user-input sanitizer coverage, live parser extraction, live DB/R2 research-run writes, live replay execution, and frontend research library remain absent | The repo can now search synthetic announcements, retrieve bounded citation excerpts, sanitize document-origin malicious instructions, run deterministic semantic chunk retrieval, compare schema-bound filing numbers, plan complete immutable research-run snapshots, and compare saved/current run snapshots through dedicated backend surfaces, but it cannot yet render the research library or execute live replay jobs | Revisit after Claude returns the frontend research library or when live DB/R2 persistence and replay execution are authorized |
| Sprint 2.3 Remote MCP OAuth and Developer Console | `mcp_endpoint_default_deny` backend scaffold now covers `@aiphabee/mcp-runtime`, `GET /mcp/runtime`, `POST /mcp`, planned `initialize` / `tools/list` / `tools/call` method handling, Origin validation, Gate 0 MCP/API redistribution default-deny, Web rights separation from MCP machine-readable rights, empty `tools/list` while rights are unconfirmed, and `DATA_NOT_LICENSED` for blocked `tools/call`; `mcp_oauth_pkce` now covers `GET /mcp/oauth/runtime`, authorize/token/revoke plan routes, PRD §9.7 scope catalog, PKCE `S256`, clear revocable scope consent, no-live token exchange, third-party token passthrough blocked, and revoke future-call denial; `mcp_api_key` now covers `GET /mcp/api-keys/runtime`, create/rotate/revoke plan routes, server-to-server only posture, hash-only storage metadata, one-time display metadata, raw key rejection, IP allowlist validation, rotation old-key future-call denial, and revoke future-call denial; `mcp_tool_schema_validation` now covers `tools/call` argument object validation, required/anyOf field validation, additionalProperties=false rejection, input/output schema IDs, and structuredContent/outputSchema no-live validation metadata; `mcp_tool_versioning` now covers public `tool@1` versions, active deprecation metadata, 90-day minimum notice, 180-day previous-major support policy, and breaking-change-new-major policy; `mcp_pagination_limits` now covers Tool Registry retrieval metadata, `tools/list` retrieval limit descriptors, `tools/call` `bounded_retrieval`, cursor metadata, max-row rejection, and time-window rejection before live execution; `mcp_usage_envelope` now covers response `usage` request_id/remaining credits, `tools/call` `usage_envelope`, estimated credits, no-live usage ledger event plan, quota display snapshot, and reconciliation target; live OAuth provider, token storage, live key generation, persistent key hash table, auth middleware, live tool execution, persistent cursor store, live usage ledger read/write, billing reconciliation, rate/concurrency limiter integration, Developer Console, and external SDK/Inspector smoke remain absent | The repo now has a concrete default-deny `/mcp` protocol surface plus testable OAuth/PKCE, API key lifecycle, schema validation, tool versioning, bounded retrieval, and usage envelope contracts, but it still cannot authenticate or execute Remote MCP calls live | Revisit with MCP-08 standard error code integration because clients still need one stable machine-readable error taxonomy across auth/right/limit/data/system failures before live execution |
| External tracker sync for P0 traceability | Repo-local `AIP-P0-*` issue refs now exist, but no external tracker has been selected | Release governance can use the repo ledger, but GitHub/Jira/Linear dashboards will not show the work automatically | External tracker is selected or the team requests import/sync |
| Remote branch reconciliation before push | Local `main` is ahead and behind origin | Push may fail or conflict | Fetch/rebase/merge remote commit before pushing local commits |
