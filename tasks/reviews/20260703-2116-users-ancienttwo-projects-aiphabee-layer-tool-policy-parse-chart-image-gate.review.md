# Task Review: users-ancienttwo-projects-aiphabee-layer-tool-policy-parse-chart-image-gate

> **Status**: Passed
> **Plan**: plans/plan-20260703-2116-users-ancienttwo-projects-aiphabee-layer-tool-policy-parse-chart-image-gate.md
> **Contract**: tasks/contracts/20260703-2116-users-ancienttwo-projects-aiphabee-layer-tool-policy-parse-chart-image-gate.contract.md
> **Notes File**: tasks/notes/20260703-2116-users-ancienttwo-projects-aiphabee-layer-tool-policy-parse-chart-image-gate.notes.md
> **Checks File**: .ai/harness/checks/latest.json
> **Last Updated**: 2026-07-03 21:33
> **Recommendation**: pass

## Human Review Card

- Verdict: pass
- Change type: code-change
- Intended files changed: `packages/agent-runtime/src/index.ts`, `packages/tool-registry/src/index.ts`, `apps/worker/src/index.ts`, focused tests, and task artifacts.
- Actual files changed: intended files plus `packages/mcp-runtime/src/index.ts` and `packages/data-access-gateway/src/index.ts` for registry ripple containment.
- Commands passed: see Verification Evidence.
- External acceptance: unavailable; local contract and unit/typecheck gates passed.
- Residual risks: no live chart parsing or FastClaw/E2B execution path is implemented in this slice.
- Reviewer action required: inspect diff for policy semantics and confirm MCP non-exposure is the desired boundary.
- Rollback: revert this task's diff in the active worktree.

## Mode Evidence

- Selected route: implementation from approved captured plan.
- P1 map: Agent Runtime owns policy contract and planning helpers; Tool Registry owns versioned tool identity; Worker owns HTTP pre-planning enforcement; MCP Runtime owns MCP schema/list/call exposure; Data Gateway owns P0 rights matrix coverage.
- P2 trace: `/agent/runs/plan` parses explicit layer/mode/context fields, builds route readback, evaluates `evaluateAgentLayerToolPolicy`, returns 403 with policy headers on blocked tools, or creates the dry-run tool-loop plan with `layer_tool_policy` readback.
- P3 decision rationale: the smallest coherent change is a deterministic pre-planning policy gate plus registry/readback updates. It preserves fail-closed behavior and avoids inferring Research intent from requested tools or prompt content.

## Verification Evidence

- Waza `/check` run: not invoked; local equivalent review card completed.
- Commands run:
- `npx vitest run packages/agent-runtime/src/index.test.ts`
- `npx vitest run packages/agent-runtime/src/parse-chart-image`
- `npx vitest run packages/tool-registry/src/index.test.ts`
- `npx vitest run packages/mcp-runtime/src/index.test.ts`
- `npx vitest run packages/data-access-gateway/src/index.test.ts`
- `npx vitest run apps/worker/src/index.test.ts`
- `npm run typecheck --workspace @aiphabee/agent-runtime`
- `npm run typecheck --workspace @aiphabee/tool-registry`
- `npm run typecheck --workspace @aiphabee/mcp-runtime`
- `npm run typecheck --workspace @aiphabee/data-access-gateway`
- `npm run typecheck --workspace @aiphabee/worker`
- Manual checks:
- Generic `parse_chart_image` blocked with `layer_not_allowed`.
- Research `parse_chart_image` allowed only with `technical_analysis`, tenant id, and image ref.
- Research missing image context blocked with `image_ref_required`.
- Unknown tools blocked with `unknown_tool`.
- MCP schema/list does not include `parse_chart_image`; MCP direct call rejects it as `TOOL_NOT_REGISTERED`.
- Supporting artifacts: implementation notes and updated contract.
- Implementation notes reviewed: yes.
- Run snapshot: `.ai/harness/runs/` if generated by `verify-contract`.

## External Acceptance Advice

> **External Acceptance**: unavailable
> **External Reviewer**:
> **External Source**:
> **External Started**:
> **External Completed**:

- P1 blockers: none.
- P2 advisories: later live execution must bind to real tenant-owned image storage and provenance before enabling runner calls.
- Acceptance checklist: pass.

## Behavior Diff Notes

- `parse_chart_image` is now a registered tool name with version/schema metadata.
- Agent Runtime exposes a layer tool policy contract and evaluates requested tools before run planning.
- Worker returns `SCOPE_DENIED` plus `x-aiphabee-tool-policy-*` headers for denied requested tools.
- MCP remains default-deny and does not expose non-MCP chart parsing.
- Data Gateway P0 rights coverage count is now 24.

## Residual Risks / Follow-ups

- No production auth/session or actual chart execution path was added.
- The current gate accepts explicit tenant/image references only as preconditions; later slices must bind those references to durable tenant-owned image records.

## Scorecard

| Dimension | Score | Notes |
|-----------|-------|-------|
| Functionality | 8/10 | Required pre-planning allow/deny cases and unknown-tool behavior pass. |
| Product depth | 7/10 | Correctly separates Agent Research chart access from Generic and MCP exposure. |
| Design quality | 8/10 | Maintains shared registry while keeping policy authority in Agent Runtime. |
| Code quality | 8/10 | Focused helpers, explicit tests, typechecks green. |

## Failing Items

- None.

## Retest Steps

- Re-run the commands listed in Verification Evidence.
- Re-run `repo-harness run verify-contract --contract tasks/contracts/20260703-2116-users-ancienttwo-projects-aiphabee-layer-tool-policy-parse-chart-image-gate.contract.md --strict`.

## Summary

- Pass. The slice implements the approved `parse_chart_image` Research-only gate without live execution or compatibility inference.
