Institution / quality rating on a 5-star scale — honey-filled stars with fractional support.

```jsx
<RatingStars value={4.5} showValue reviews={128} />
<RatingStars value={3} size={20} />
```

Props: `value`, `count` (default 5), `size`, `showValue`, `reviews`, `color`, `emptyColor`.
